(function c22Front(){
"use strict";
const data=window.PENGUIN_CONVEXITY_C22,tracking=window.PENGUIN_CONVEXITY_C22_TRACKING;
if(!data||data.schemaVersion!=="c2.2-front-v1")return;
const $=(s,r=document)=>r.querySelector(s),esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const fmtTime=v=>{if(!v)return"暂无";const d=new Date(v);return Number.isNaN(d.valueOf())?String(v):new Intl.DateTimeFormat("zh-CN",{month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit",hour12:false}).format(d)};
const fmtNum=v=>v==null?"暂无":new Intl.NumberFormat("zh-CN",{maximumFractionDigits:2,notation:Math.abs(Number(v))>=1000000?"compact":"standard"}).format(v);
const stateLabel={convexity_clue:"凸性线索",active_project:"活跃项目",observing:"观察中",awaiting_first_tracking:"等待首轮跟踪"};
const relLabel={A:"新项目新币",B:"老项目新资产",C:"项目关系未确认"};
const directionLabel={improving:"改善",stable:"稳定",weakening:"减弱",no_measured:"尚无实测",unavailable:"不可用"};
const statusLabel={pass:"通过",fail:"未通过",pending:"待更新",formed:"已形成",forming:"形成中",not_formed:"未形成",unavailable:"不可用"};
const main=$(".c19-front-main")||$(".c19-detail-main")||$(".c2-front-main")||$(".c2-detail-main")||$("main");if(!main)return;main.className="c21-main";
const params=new URLSearchParams(location.search),path=location.pathname.split("/").pop(),mode=path==="project-detail.html"?"detail":path==="change-explanations.html"?"changes":params.get("view")||"home";
document.querySelectorAll(".c19-front-nav a").forEach(a=>{a.classList.toggle("is-active",a.dataset.frontMode===mode);if(a.dataset.frontMode==="changes")a.href="change-explanations.html"});
const trackingItems=new Map((tracking?.items||[]).map(item=>[item.assetId,item]));
function head(kicker,title,intro){return`<header class="c21-page-head"><div><span class="c21-kicker">${esc(kicker)}</span><h1>${esc(title)}</h1><p class="c21-lead">${esc(intro)}</p><span class="c21-time">本页数据更新到 ${esc(fmtTime(data.sourceCutoffAt))}</span></div></header>`}
function badge(item){return`<span class="c21-badge" data-state="${esc(item.trackingState)}">${esc(stateLabel[item.trackingState]||item.trackingState)}</span>`}
function evidenceText(item){const p=item.productEvidenceSummary||{};if(!p.hasAnyQualifyingEvidence)return"暂无合格产品证据";return p.plainSummary||((p.qualifyingTypes||[]).join("、")||"已有产品证据")}
function card(item){const pathCount=item.strongPathSummary?.formedCount||0;return`<a class="c21-card" href="${esc(item.detailHref)}" data-detail-link><div class="c21-card-top"><div class="c21-name"><strong>${esc(item.canonicalName)}</strong><span>${esc(item.symbol)}</span></div>${badge(item)}</div><p class="c21-reason">${esc(item.sortReason||"筛选已通过，等待跟踪解释")}</p><div class="c21-meta"><span>${esc(item.chainId)}</span><span>T0后第 ${esc(item.ageDays)} 天</span><span>${esc(relLabel[item.relationshipClass]||item.relationshipClass)}</span><span>${esc(item.ageBand==="age_0_30"?"0—30天":"31—90天")}</span></div><div class="c21-row-sub"><span>产品证据<strong>${esc(evidenceText(item))}</strong></span><span>强路径<strong>${pathCount?`${pathCount} 条已形成`:"尚未形成"}</strong></span></div><span class="c21-detail-cta">查看详情 →</span></a>`}
function empty(title="当前没有符合筛选条件的项目"){return`<div class="c21-empty"><h2>${esc(title)}</h2><p>这里不使用模板项目补位。筛选结果为真实零结果，来源问题只在后台更新中心说明。</p></div>`}
function saveReturn(){sessionStorage.setItem("c22-return",JSON.stringify({url:location.pathname.split("/").pop()+location.search,scrollY:window.scrollY}))}
function bindLinks(){document.querySelectorAll("[data-detail-link]").forEach(a=>a.addEventListener("click",saveReturn))}
function home(){const c=data.coverageSummary||{},l=data.lifecycleCounts||{},s=data.trackingStateCounts||{},items=data.items.slice(0,8);main.innerHTML=head("90天新币筛选 × 凸性跟踪","先看已通过第一关的项目","新币筛选负责把0—90天且通过宽硬门槛的项目列出来；凸性跟踪再用交易、流动性、供应、使用和风险证据说明当前状态。")+`<section class="c21-stat-grid"><div class="c21-stat"><small>前台合格项目</small><strong>${c.frontVisibleCount||items.length}</strong></div><div class="c21-stat"><small>0—30天</small><strong>${l.age_0_30||0}</strong></div><div class="c21-stat"><small>31—90天</small><strong>${l.age_31_90||0}</strong></div><div class="c21-stat"><small>凸性线索</small><strong>${s.convexity_clue||0}</strong></div><div class="c21-stat"><small>活跃项目</small><strong>${s.active_project||0}</strong></div><div class="c21-stat"><small>观察中</small><strong>${s.observing||0}</strong></div></section><nav class="c21-home-states" aria-label="四种跟踪状态">${Object.entries(stateLabel).map(([k,v])=>`<a href="candidate-pool.html?view=all&state=${k}"><span>${esc(v)}</span><strong>${s[k]||0}</strong></a>`).join("")}</nav><section class="c21-section"><div class="c21-section-head"><div><span class="c21-kicker">最多8个真实项目</span><h2>今天先看</h2></div><a href="candidate-pool.html?view=all">查看全部机会 →</a></div>${items.length?`<div class="c21-grid">${items.map(card).join("")}</div>`:empty()}</section><section class="c21-section"><div class="c21-section-head"><div><span class="c21-kicker">两个模块，各负其责</span><h2>页面怎么读</h2></div></div><div class="c21-method-grid"><article class="c21-method-card"><h3>90天新币筛选</h3><p>只负责确认T0、日龄、A/B/C身份、硬门槛和产品证据。它是第一关，不把所有项目直接叫作凸性项目。</p></article><article class="c21-method-card"><h3>凸性跟踪</h3><p>只负责真实市场与项目证据、四条强路径、动态后验方向和可信度。贝叶斯结果用于排序和变化解释，不单独触发凸性线索。</p></article></div></section>`;bindLinks()}
function all(){let state=params.get("state")||"all",age=params.get("ageBand")||"all",rel=params.get("relationship")||"all",chain=params.get("chain")||"all",evidence=params.get("evidenceType")||"all",sort=params.get("sort")||"system",q=params.get("q")||"",page=Math.max(1,Number(params.get("page")||1));const chains=[...new Set(data.items.map(x=>x.chainId))].sort();main.innerHTML=head("全部机会","全部通过第一关的项目","所有项目都先通过宽硬门槛，再按生命周期和凸性跟踪状态分类。每页20条，不按综合分隐藏项目。")+`<div class="c21-tabs">${[["all","全部",data.items.length],...Object.entries(stateLabel).map(([k,v])=>[k,v,data.trackingStateCounts[k]||0])].map(([k,l,n])=>`<button class="c21-tab ${state===k?"is-active":""}" data-state-filter="${k}">${esc(l)} ${n}</button>`).join("")}</div><div class="c21-toolbar"><input id="c22Search" type="search" value="${esc(q)}" placeholder="搜索项目、符号或链"><select id="c22Age"><option value="all">全部年龄</option><option value="0_30">0—30天</option><option value="31_90">31—90天</option></select><select id="c22Rel"><option value="all">A/B/C全部</option><option value="A">A 新项目新币</option><option value="B">B 老项目新资产</option><option value="C">C 关系未确认</option></select><select id="c22Chain"><option value="all">全部链</option>${chains.map(x=>`<option value="${esc(x)}">${esc(x)}</option>`).join("")}</select><select id="c22Evidence"><option value="all">全部产品证据</option><option value="github">仅代码证据</option><option value="deployed_product">已部署产品</option><option value="business">已有业务数据</option><option value="token_utility">代币功能已执行</option><option value="product_usage">已有链上使用</option></select><select id="c22Sort"><option value="system">系统排序</option><option value="latest_change">最新变化</option><option value="t0_desc">T0最近</option><option value="liquidity_desc">流动性从高到低</option></select><span id="c22Result" class="c21-result"></span></div><div id="c22List" class="c21-list"></div><div id="c22Pages" class="c21-pagination"></div>`;$("#c22Age").value=age;$("#c22Rel").value=rel;$("#c22Chain").value=chain;$("#c22Evidence").value=evidence;$("#c22Sort").value=sort;const metric=(x,k)=>Number(x.marketSummary?.[k]??-1),changed=x=>new Date(x.latestMaterialChange?.changedAt||0).valueOf();function url(){const p=new URLSearchParams({view:"all"});if(state!=="all")p.set("state",state);if(age!=="all")p.set("ageBand",age);if(rel!=="all")p.set("relationship",rel);if(chain!=="all")p.set("chain",chain);if(evidence!=="all")p.set("evidenceType",evidence);if(sort!=="system")p.set("sort",sort);if(q)p.set("q",q);if(page>1)p.set("page",page);history.replaceState(null,"",`candidate-pool.html?${p}`)}function render(){let rows=data.items.filter(x=>(state==="all"||x.trackingState===state)&&(age==="all"||x.ageBand===`age_${age}`)&&(rel==="all"||x.relationshipClass===rel)&&(chain==="all"||x.chainId===chain)&&(evidence==="all"||(x.productEvidenceSummary?.qualifyingTypes||[]).includes(evidence))&&(!q||`${x.canonicalName} ${x.symbol} ${x.chainId}`.toLowerCase().includes(q.toLowerCase())));if(sort==="latest_change")rows.sort((a,b)=>changed(b)-changed(a));if(sort==="t0_desc")rows.sort((a,b)=>new Date(b.effectiveT0)-new Date(a.effectiveT0));if(sort==="liquidity_desc")rows.sort((a,b)=>metric(b,"liquidityUsd")-metric(a,"liquidityUsd"));const pages=Math.max(1,Math.ceil(rows.length/20));page=Math.min(page,pages);const slice=rows.slice((page-1)*20,page*20);$("#c22Result").textContent=`${rows.length} 个项目 · 第 ${page}/${pages} 页`;$("#c22List").innerHTML=slice.map(card).join("")||empty();$("#c22Pages").innerHTML=Array.from({length:pages},(_,i)=>`<button data-page="${i+1}" ${i+1===page?'aria-current="page"':""}>${i+1}</button>`).join("");url();bindLinks();document.querySelectorAll("[data-page]").forEach(b=>b.onclick=()=>{page=Number(b.dataset.page);render();scrollTo({top:0,behavior:"smooth"})})}document.querySelectorAll("[data-state-filter]").forEach(b=>b.onclick=()=>{state=b.dataset.stateFilter;page=1;render()});[["#c22Age",v=>age=v],["#c22Rel",v=>rel=v],["#c22Chain",v=>chain=v],["#c22Evidence",v=>evidence=v],["#c22Sort",v=>sort=v]].forEach(([s,set])=>$(s).onchange=e=>{set(e.target.value);page=1;render()});$("#c22Search").oninput=e=>{q=e.target.value.trim();page=1;render()};render();try{const saved=JSON.parse(sessionStorage.getItem("c22-return")||"null");if(saved&&saved.url===location.pathname.split("/").pop()+location.search)setTimeout(()=>scrollTo(0,saved.scrollY||0),0)}catch(_){} }
function changes(){const rows=[...(data.changes||[])],size=20,pages=Math.max(1,Math.ceil(rows.length/size));let page=Math.max(1,Math.min(pages,Number(params.get("page")||1)));function render(){const slice=rows.slice((page-1)*size,page*size);main.innerHTML=head("会改变判断的事件","重要变化","这里只显示身份、硬门槛、产品证据、跟踪状态、强路径、后验方向、可信度和风险的变化；普通行情流水不会堆进来.")+(rows.length?`<p class="c21-result">${rows.length} 条重要变化 · 第 ${page}/${pages} 页</p><div class="c21-change-list">${slice.map(x=>`<article class="c21-change"><div class="c21-card-top"><div><span class="c21-kicker">发生了什么</span><h2>${esc(x.projectId||x.assetId||"项目")}：${esc(x.currentValue||x.changeType)}</h2></div><span class="c21-time">${esc(fmtTime(x.changedAt))}</span></div><dl><div><dt>之前</dt><dd>${esc(x.previousValue||"暂无")}</dd></div><div><dt>现在</dt><dd>${esc(x.currentValue||"暂无")}</dd></div></dl><p><strong>为什么影响判断：</strong>${esc(x.whyItMatters)}</p><p class="c21-source-note">数据更新到 ${esc(fmtTime(x.sourceCutoffAt))} · <a href="${esc(x.detailHref)}" data-detail-link>查看项目证据</a></p></article>`).join("")}</div><div class="c21-pagination">${Array.from({length:pages},(_,i)=>`<button data-change-page="${i+1}" ${i+1===page?'aria-current="page"':""}>${i+1}</button>`).join("")}</div>`:empty("当前没有需要展示的重要变化"));bindLinks();document.querySelectorAll("[data-change-page]").forEach(b=>b.onclick=()=>{page=Number(b.dataset.changePage);history.replaceState(null,"",`change-explanations.html?page=${page}`);render();scrollTo({top:0,behavior:"smooth"})})}render()}
function method(){main.innerHTML=head("程序边界公开","我们如何判断","C2.2 仍是一套产品：新币筛选负责第一关，凸性跟踪负责第二关。两者通过assetId合并，失败时保留各自上一份完整快照。")+`<div class="c21-method-grid"><article class="c21-method-card"><h2>第一关：新币筛选</h2><p>按真实T0确认0—90天范围、A/B/C关系、宽硬门槛和产品证据。D只有代币不进入前台。通过第一关的项目全部保留，不会被总分隐藏。</p></article><article class="c21-method-card"><h2>第二关：凸性跟踪</h2><p>按交易、流动性、地址与供应、产品使用、风险和异常更新四条强证据路径。至少两条强路径且必须包含交易与流动性，才能显示凸性线索。</p></article><article class="c21-method-card"><h2>动态后验</h2><p>同链同日龄比较组、P1/P99截尾、稳健先验和窗口证据会产生五因子后验。它只用于排序、变化和时间外校准，不等于投资建议，也不单独形成凸性线索。</p></article><article class="c21-method-card"><h2>缺失怎么显示</h2><p>前台不把接口额度、来源故障或程序错误写成项目状态。没有首份完整跟踪时显示“等待首轮跟踪”；具体失败原因只在后台更新中心按来源和作业展示。</p></article><article class="c21-method-card"><h2>两个稳定边界</h2><p>筛选失败时跟踪继续使用上一份完整候选清单；跟踪失败时前台保留上一份完整跟踪结果。组合只按assetId连接，不用名称、Symbol或数组顺序。</p></article><article class="c21-method-card c21-boundary"><h2>程序做不到</h2><p>不能证明项目安全、不能发现所有操纵、不能预测收益或未来上涨，也不能代替你的投资判断。GitHub仅是代码证据，不代表产品已部署或被采用。</p></article></div><p class="c21-method-ending">页面显示的是已接入数据范围内的机器研究线索，所有结论都带数据截止时间。</p>`}
function detail(){const item=data.items.find(x=>x.projectId===params.get("id")||x.assetId===params.get("assetId"));let back="candidate-pool.html?view=all";try{const saved=JSON.parse(sessionStorage.getItem("c22-return")||"null");if(saved?.url)back=saved.url}catch(_){}if(!item){main.innerHTML=`<a class="c21-back" href="${esc(back)}">← 返回</a>${head("项目详情","当前完整快照中没有这个项目","它可能已经退出90天范围，或链接来自旧快照。")}`;return}const t=trackingItems.get(item.assetId)||{},paths=t.strongPaths||[],hard=item.hardGateSummary||{},evidence=item.productEvidenceSummary||{},market=t.marketHistory?.latest,risk=t.riskAndAnomalies||{};main.innerHTML=`<a class="c21-back" href="${esc(back)}">← 返回原列表</a><header class="c21-detail-head"><span class="c21-kicker">${esc(item.relationshipClass)} · ${esc(relLabel[item.relationshipClass]||item.relationshipClass)}</span><div class="c21-card-top"><div><h1>${esc(item.canonicalName)} <small>${esc(item.symbol)}</small></h1><p class="c21-lead">${esc(item.sortReason)}</p></div>${badge(item)}</div><div class="c21-meta"><span>${esc(item.chainId)}</span><span>T0后第 ${esc(item.ageDays)} 天</span><span>${esc(item.effectiveT0)}</span><span>数据到 ${esc(fmtTime(item.dataCutoffAt||data.sourceCutoffAt))}</span></div></header><div class="c21-detail-layout"><div><section class="c21-panel"><h2>第一关：为什么进入筛选清单</h2><div class="c21-checks">${(hard.checks||[]).map(x=>`<div class="c21-check"><div class="c21-check-head"><strong>${esc(x.label)}</strong><span class="c21-status-${esc(x.status)}">${esc(statusLabel[x.status]||x.status)}</span></div><p>${esc(x.reason)}</p><small class="c21-source-note">${esc((x.sourceNames||[]).join("、")||"本项没有可展示来源")} · ${esc(fmtTime(x.observedAt))}</small></div>`).join("")||empty("没有可展示的硬门槛明细")}</div></section><section class="c21-panel c21-section"><h2>第二关：四条强证据路径</h2><div class="c21-paths">${paths.map(x=>`<div class="c21-path"><div class="c21-check-head"><strong>${esc(x.label||x.pathCode)}</strong><span class="c21-status-${esc(x.status)}">${esc(statusLabel[x.status]||x.status)}</span></div><p>${esc(x.plainReason||"暂无解释")}</p><div class="c21-metrics">${(x.supportingMetrics||[]).map(m=>`<span class="c21-metric">${esc(m.label)}：${esc(fmtNum(m.value))}${esc(m.unit||"")}</span>`).join("")}</div>${x.unknowns?.length?`<p class="c21-source-note">仍未知：${esc(x.unknowns.join("、"))}</p>`:""}</div>`).join("")||empty("尚无完整路径结果")}</div></section></div><aside><section class="c21-panel"><h2>当前事实</h2><dl class="c21-facts"><div><dt>T0</dt><dd>${esc(fmtTime(item.effectiveT0))}</dd></div><div><dt>生命周期</dt><dd>${esc(item.ageBand==="age_0_30"?"0—30天":"31—90天")}</dd></div><div><dt>产品证据</dt><dd>${esc(evidenceText(item))}</dd></div><div><dt>市场</dt><dd>${esc(market?`流动性 ${fmtNum(market.liquidityUsd)} USD`:"尚无完整市场跟踪")}</dd></div><div><dt>硬风险</dt><dd>${esc(risk.plainReason||"暂无可展示风险说明")}</dd></div></dl></section>${evidence.github?.status==="qualifying"?`<section class="c21-panel c21-section c21-boundary"><h2>仅代码证据</h2><p>${esc(evidence.github.repository||evidence.github.organization)}</p><p>${esc(evidence.github.boundaryNote||"GitHub只证明代码证据，不证明产品部署或采用。")}</p>${evidence.github.repositoryUrl?`<a href="${esc(evidence.github.repositoryUrl)}">打开仓库</a>`:""}</section>`:""}<section class="c21-panel c21-section"><h2>五因子方向</h2><div class="c21-metrics">${(item.factorDirections||[]).map(x=>`<span class="c21-metric">${esc(x.factor)}：${esc(directionLabel[x.direction]||x.direction)}</span>`).join("")||"尚无实测"}</div><p class="c21-source-note">动态后验用于排序和变化解释，不单独定义凸性线索。</p></section></aside></div>`}
({home,all,changes,method,detail}[mode]||home)();
})();
