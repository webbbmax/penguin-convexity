(function c21Front(){
"use strict";
const data=window.PENGUIN_CONVEXITY_C21;if(!data||data.schemaVersion!=="c2.1-front-snapshot-v1")return;
if(window.PENGUIN_CONVEXITY_C22?.schemaVersion==="c2.2-front-v1")return;
const $=(s,r=document)=>r.querySelector(s),esc=(v)=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const fmtTime=v=>{if(!v)return"尚无成功时间";const d=new Date(v);return Number.isNaN(d.valueOf())?String(v):new Intl.DateTimeFormat("zh-CN",{month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit",hour12:false}).format(d)};
const fmtNum=v=>v==null?"暂无":new Intl.NumberFormat("zh-CN",{maximumFractionDigits:2,notation:Math.abs(Number(v))>=1000000?"compact":"standard"}).format(v);
const stateLabel={convexity_clue:"凸性线索",active_project:"活跃项目",early_observation:"新发观察",continuous_observation:"持续观察",data_limited:"数据受限"};
const relLabel={A:"新项目新币",B:"老项目新资产",C:"项目关系未确认",D:"只有代币"};
const changeTypeLabel={front_eligibility:"前台资格",display_state:"观察状态",data_confidence:"数据可信度"};
const changeValueLabel={...stateLabel,limited:"部分数据受限",interrupted:"关键数据中断",sufficient:"数据较充分"};
const statusLabel={pass:"通过",fail:"未通过",pending:"待更新",stale:"数据受限",unsupported:"暂不支持",formed:"已形成",forming:"形成中",not_formed:"未形成",unavailable:"不可用"};
const directionLabel={improving:"改善",stable:"稳定",weakening:"减弱",unavailable:"不可用"};
const confidenceLabel={sufficient:"数据较充分",limited:"部分数据受限",interrupted:"关键数据中断"};
const main=$(".c19-front-main")||$(".c19-detail-main")||$(".c2-front-main")||$(".c2-detail-main")||$("main");if(!main)return;main.className="c21-main";
document.querySelectorAll(".c19-front-nav a").forEach(a=>{a.classList.remove("is-active");if(a.dataset.frontMode==="changes")a.href="change-explanations.html";});
const params=new URLSearchParams(location.search),path=location.pathname.split("/").pop(),mode=path==="project-detail.html"?"detail":path==="change-explanations.html"?"changes":params.get("view")||"home";
const nav=document.querySelector(`[data-front-mode="${mode}"]`);if(nav)nav.classList.add("is-active");
function head(kicker,title,intro){const shownKicker=kicker==="90天新发项目自动筛选"?"90天新币筛选":kicker;return `<header class="c21-page-head"><div><span class="c21-kicker">${esc(shownKicker)}</span><h1>${esc(title)}</h1><p class="c21-lead">${esc(intro)}</p><span class="c21-time">本页数据更新到 ${esc(fmtTime(data.sourceCutoffAt))}</span></div></header>`}
function badge(item){return `<span class="c21-badge" data-state="${esc(item.displayState.code)}">${esc(item.displayState.label||stateLabel[item.displayState.code])}</span>`}
function card(item){return `<a class="c21-card" href="${esc(item.detailUrl)}" data-detail-link><div class="c21-card-top"><div class="c21-name"><strong>${esc(item.canonicalName)}</strong><span>${esc(item.symbol)}</span></div>${badge(item)}</div><p class="c21-reason">${esc(item.displayState.reason)}</p><div class="c21-meta"><span>${esc(item.chainId)}</span><span>发行第 ${esc(item.ageDays)} 天</span><span>${esc(item.relationshipLabel)}</span><span>${esc(item.productEvidence.plainSummary)}</span></div><span class="c21-detail-cta">查看详情 →</span></a>`}
function empty(){const blockers=Object.entries(data.blockerCounts||{}).sort((a,b)=>b[1]-a[1]).slice(0,6);return `<div class="c21-empty"><h2>当前没有项目通过全部前台观察门槛</h2><p>系统没有用模板项目补位。以下是后台真实阻断数量；额度或来源故障不会被写成“项目资料不足”。</p><div class="c21-blockers">${blockers.map(([k,v])=>`<div class="c21-blocker"><strong>${esc(v)}</strong><span>${esc(k)}</span></div>`).join("")||"<span>尚未生成阻断统计。</span>"}</div></div>`}
function saveReturn(){const value={url:location.pathname.split("/").pop()+location.search,scrollY:window.scrollY};sessionStorage.setItem("c21-return",JSON.stringify(value))}
function bindLinks(){document.querySelectorAll("[data-detail-link]").forEach(a=>a.addEventListener("click",saveReturn))}
function home(){const c=data.coverageSummary,s=data.statusCounts,items=[...data.items].slice(0,8),states=Object.keys(stateLabel);main.innerHTML=head("90天新发项目自动筛选","先看程序已经证实了什么","这里展示全部通过宽硬门槛的项目。状态来自真实数据路径，不由综合分或项目年龄单独决定。")+`<section class="c21-stat-grid"><div class="c21-stat"><small>前台合格项目</small><strong>${c.frontVisibleCount}</strong></div><div class="c21-stat"><small>24小时新增</small><strong>${c.added24hCount||0}</strong></div><div class="c21-stat"><small>凸性线索</small><strong>${s.convexity_clue||0}</strong></div><div class="c21-stat"><small>活跃项目</small><strong>${s.active_project||0}</strong></div><div class="c21-stat"><small>数据受限</small><strong>${s.data_limited||0}</strong></div><div class="c21-stat"><small>T0已核验</small><strong>${c.t0VerifiedCount||0}</strong></div></section><nav class="c21-home-states" aria-label="五种观察状态">${states.map(k=>`<a href="candidate-pool.html?view=all&state=${k}"><span>${esc(stateLabel[k])}</span><strong>${s[k]||0}</strong></a>`).join("")}</nav><section class="c21-section"><div class="c21-section-head"><div><span class="c21-kicker">最多8个真实项目</span><h2>今天先看</h2></div><a href="candidate-pool.html?view=all">查看全部机会 →</a></div>${items.length?`<div class="c21-grid">${items.map(card).join("")}</div>`:empty()}</section><section class="c21-section"><div class="c21-section-head"><div><span class="c21-kicker">不要误读</span><h2>这页不是投资推荐榜</h2></div></div><div class="c21-method-grid"><article class="c21-method-card"><h3>凸性线索</h3><p>至少两条强证据路径同时形成，而且必须包括交易与流动性。它表示结构值得继续研究，不预测一定上涨。</p></article><article class="c21-method-card"><h3>活跃项目</h3><p>出现了可复算的真实活动，但还没有形成两条强证据路径。它不是“次一级买入信号”。</p></article></div></section>`;bindLinks()}
function all(){let state=params.get("state")||"all",age=params.get("ageBand")||"all",rel=params.get("relationship")||"all",chain=params.get("chain")||"all",evidence=params.get("evidenceType")||"all",dataStatus=params.get("dataStatus")||"all",sort=params.get("sort")||"system",q=params.get("q")||"",page=Math.max(1,Number(params.get("page")||1));
const chains=[...new Set(data.items.map(x=>x.chainId))].sort();main.innerHTML=head("全部机会","全部通过门槛的项目","这里展示全部通过前台观察门槛的项目，不按综合分隐藏。每页固定20条，没有无限滚动。")+`<div class="c21-tabs">${[["all","全部",data.items.length],...Object.keys(stateLabel).map(k=>[k,stateLabel[k],data.statusCounts[k]||0])].map(([k,l,n])=>`<button class="c21-tab ${state===k?"is-active":""}" data-state-filter="${k}">${l} ${n}</button>`).join("")}</div><div class="c21-toolbar"><input id="c21Search" type="search" value="${esc(q)}" placeholder="搜索项目、符号或链"><select id="c21Age"><option value="all">全部年龄</option><option value="0_30">0—30天</option><option value="31_90">31—90天</option></select><select id="c21Rel"><option value="all">A/B/C全部</option><option value="A">A 新项目新币</option><option value="B">B 老项目新资产</option><option value="C">C 关系未确认</option></select><select id="c21Chain"><option value="all">全部链</option>${chains.map(x=>`<option value="${esc(x)}">${esc(x)}</option>`).join("")}</select><select id="c21Evidence"><option value="all">全部产品证据</option><option value="github">仅代码证据</option><option value="deployed_product">已部署产品</option><option value="business">已有业务数据</option><option value="token_utility">代币功能已执行</option><option value="product_usage">已有链上使用</option></select><select id="c21DataStatus"><option value="all">全部数据状态</option><option value="healthy">更新正常</option><option value="limited">部分数据受限</option><option value="interrupted">关键数据中断</option></select><select id="c21Sort"><option value="system">系统排序</option><option value="latest_change">最新变化</option><option value="t0_desc">T0最近</option><option value="liquidity_desc">流动性从高到低</option><option value="volume_desc">成交额从高到低</option></select><span id="c21Result" class="c21-result"></span></div><div id="c21List" class="c21-list"></div><div id="c21Pages" class="c21-pagination"></div>`;
$("#c21Age").value=age;$("#c21Rel").value=rel;$("#c21Chain").value=chain;$("#c21Evidence").value=evidence;$("#c21DataStatus").value=dataStatus;$("#c21Sort").value=sort;
const itemDataStatus=x=>x.sourceImpact?.status==="interrupted"?"interrupted":x.sourceImpact?.status==="healthy"?"healthy":"limited",metric=(x,k)=>Number(x.marketSnapshot?.[k]??-1),changedAt=x=>new Date(x.latestMaterialChange?.changedAt||0).valueOf();
function updateUrl(){const p=new URLSearchParams({view:"all"});if(state!=="all")p.set("state",state);if(age!=="all")p.set("ageBand",age);if(rel!=="all")p.set("relationship",rel);if(chain!=="all")p.set("chain",chain);if(evidence!=="all")p.set("evidenceType",evidence);if(dataStatus!=="all")p.set("dataStatus",dataStatus);if(sort!=="system")p.set("sort",sort);if(q)p.set("q",q);if(page>1)p.set("page",page);history.replaceState(null,"",`candidate-pool.html?${p}`)}
function render(){let rows=data.items.filter(x=>(state==="all"||x.displayState.code===state)&&(age==="all"||(age==="0_30"?x.ageDays<=30:x.ageDays>=31))&&(rel==="all"||x.relationshipClass===rel)&&(chain==="all"||x.chainId===chain)&&(evidence==="all"||x.productEvidence.qualifyingTypes.includes(evidence))&&(dataStatus==="all"||itemDataStatus(x)===dataStatus)&&(!q||`${x.canonicalName} ${x.symbol} ${x.chainId}`.toLowerCase().includes(q.toLowerCase())));if(sort==="latest_change")rows.sort((a,b)=>changedAt(b)-changedAt(a));if(sort==="t0_desc")rows.sort((a,b)=>new Date(b.t0.value)-new Date(a.t0.value));if(sort==="liquidity_desc")rows.sort((a,b)=>metric(b,"liquidityUsd")-metric(a,"liquidityUsd"));if(sort==="volume_desc")rows.sort((a,b)=>metric(b,"volumeUsd")-metric(a,"volumeUsd"));const pages=Math.max(1,Math.ceil(rows.length/20));page=Math.min(page,pages);const slice=rows.slice((page-1)*20,page*20);$("#c21Result").textContent=`${rows.length} 个项目 · 第 ${page}/${pages} 页`;$("#c21List").innerHTML=slice.map(x=>`<article class="c21-row"><a href="${esc(x.detailUrl)}" data-detail-link><div class="c21-row-top"><div class="c21-name"><strong>${esc(x.canonicalName)}</strong><span>${esc(x.symbol)} · ${esc(x.chainId)} · T0第${x.ageDays}天 · ${esc(x.relationshipClass)}</span></div>${badge(x)}</div><p class="c21-reason">${esc(x.displayState.reason)}</p><div class="c21-row-sub"><span>产品证据<strong>${esc(x.productEvidence.plainSummary)}</strong></span><span>主要风险<strong>${esc(x.riskSummary.plainReason)}</strong></span><span>数据时间<strong>${esc(fmtTime(x.observationHistory.lastSuccessfulAt))}</strong></span><span>最近变化<strong>${esc(x.latestMaterialChange?.whyItMatters||"当前没有重要变化")}</strong></span></div><span class="c21-detail-cta">查看详情 →</span></a></article>`).join("")||empty();$("#c21Pages").innerHTML=Array.from({length:pages},(_,i)=>`<button data-page="${i+1}" ${i+1===page?'aria-current="page"':""}>${i+1}</button>`).join("");updateUrl();bindLinks();document.querySelectorAll("[data-page]").forEach(b=>b.onclick=()=>{page=Number(b.dataset.page);render();scrollTo({top:0,behavior:"smooth"})})}
document.querySelectorAll("[data-state-filter]").forEach(b=>b.onclick=()=>{state=b.dataset.stateFilter;page=1;document.querySelectorAll("[data-state-filter]").forEach(x=>x.classList.toggle("is-active",x===b));render()});[["#c21Age",v=>age=v],["#c21Rel",v=>rel=v],["#c21Chain",v=>chain=v],["#c21Evidence",v=>evidence=v],["#c21DataStatus",v=>dataStatus=v],["#c21Sort",v=>sort=v]].forEach(([s,set])=>$(s).onchange=e=>{set(e.target.value);page=1;render()});$("#c21Search").oninput=e=>{q=e.target.value.trim();page=1;render()};render();try{const saved=JSON.parse(sessionStorage.getItem("c21-return")||"null");if(saved&&saved.url===location.pathname.split("/").pop()+location.search)setTimeout(()=>scrollTo(0,saved.scrollY||0),0)}catch(_){}}
function changes(){const rows=[...(data.materialChanges||[])],plain=v=>changeValueLabel[v]||v,byKey=new Map;for(const row of rows){const key=`${row.projectId}|${row.changedAt}`;if(!byKey.has(key))byKey.set(key,{key,rows:[]});byKey.get(key).rows.push(row)}const groups=[...byKey.values()].sort((a,b)=>new Date(b.rows[0].changedAt)-new Date(a.rows[0].changedAt)),size=20,pages=Math.max(1,Math.ceil(groups.length/size));let page=Math.max(1,Math.min(pages,Number(params.get("page")||1))),slice=groups.slice((page-1)*size,page*size);const render=()=>{main.innerHTML=head("会改变判断的事件","重要变化","这里只记录会改变资格、产品证据、五状态、可信度或退出范围的变化；普通价格波动不会堆进来。")+(groups.length?`<p class="c21-result">${groups.length} 组重要变化 · 第 ${page}/${pages} 页</p><div class="c21-change-list">${slice.map(group=>{const first=group.rows[0],current=plain(first.currentValue);return `<article class="c21-change"><div class="c21-card-top"><div><span class="c21-kicker">发生了什么</span><h2>${esc(first.canonicalName)} ${esc(first.symbol)}：${esc(current)}</h2></div><span class="c21-time">${esc(fmtTime(first.changedAt))}</span></div>${group.rows.map(x=>`<section class="c21-change-detail"><h3>${esc(changeTypeLabel[x.changeType]||"判断依据发生变化")}</h3><dl><div><dt>之前</dt><dd>${esc(plain(x.previousValue))}</dd></div><div><dt>现在</dt><dd>${esc(plain(x.currentValue))}</dd></div></dl><p><strong>为什么影响判断：</strong>${esc(x.whyItMatters)}</p></section>`).join("")}<p class="c21-source-note">数据更新到 ${esc(fmtTime(first.sourceCutoffAt))} · <a href="${esc(first.detailUrl)}" data-detail-link>查看项目证据</a></p></article>`}).join("")}</div><div class="c21-pagination">${Array.from({length:pages},(_,i)=>`<button data-change-page="${i+1}" ${i+1===page?'aria-current="page"':""}>${i+1}</button>`).join("")}</div>`:`<div class="c21-empty"><h2>当前没有需要展示的重要变化</h2><p>这是真实零结果，不使用普通行情或模板事件补位。</p></div>`);bindLinks();document.querySelectorAll("[data-change-page]").forEach(button=>button.onclick=()=>{page=Number(button.dataset.changePage);slice=groups.slice((page-1)*size,page*size);const query=path==="change-explanations.html"?`change-explanations.html?page=${page}`:`candidate-pool.html?view=changes&page=${page}`;history.replaceState(null,"",query);render();scrollTo({top:0,behavior:"smooth"})})};render()}
function method(){main.innerHTML=head("程序边界公开","我们如何判断","先说明程序能做到哪里，再解释页面上的状态。所有结论都能回到输入事实与冻结规则。")+`<div class="c21-method-grid"><article class="c21-method-card"><h2>1. 什么算新发</h2><p>T0 是接入范围内目前查到的最早公开领取、转让或交易证据。第0—90天进入新发池，第91天退出。发现时间、部署时间和年龄都不加分。</p></article><article class="c21-method-card"><h2>2. A/B/C/D是什么</h2><p>A是新项目新币，B是老项目新资产，C是程序确认了项目与资产但不能确定项目新旧，D只有代币。A/B/C可进入前台，D留在后台。</p></article><article class="c21-method-card"><h2>3. 先过宽硬门槛</h2><p>程序逐项核验T0、90天范围、身份与池方向、公众买卖事实、显性交易阻断和产品证据。所有通过者都会显示，综合分不能把它们藏起来。</p></article><article class="c21-method-card"><h2>4. 凸性线索</h2><p>至少两条强证据路径形成，其中必须包括交易与流动性；还要有至少3个已观察因子和两个独立来源类型。它不是收益预测。</p></article><article class="c21-method-card"><h2>5. 活跃项目</h2><p>成交、真实代码提交、产品使用或连续流动性任一达到冻结规则，就可显示为活跃项目。热度、涨幅、粉丝或仓库星标不能单独触发。</p></article><article class="c21-method-card"><h2>6. 新发观察与持续观察</h2><p>发布不足14天且尚未形成强弱线索的项目显示为新发观察；真实历史已满14天、仍在0—90天范围内但尚未形成强弱线索的项目显示为持续观察。两者都不是负面结论，未来日期也不会补成0。</p></article><article class="c21-method-card"><h2>7. 分数只负责排序</h2><p>动态五因子会随真实数据更新，用于排序和比较变化。页面不会写“多少分等于凸性线索”，缺失数据也不会补成0。</p></article><article class="c21-method-card"><h2>8. 来源失败怎么处理</h2><p>无数据、额度耗尽、来源失败、不支持、配置缺失和程序错误分别记录。关键更新失败时保留上次完整快照并标记数据受限，不把接口故障写成项目变差。</p></article><article class="c21-method-card c21-boundary"><h2>程序明确做不到</h2><p>不能保证项目安全、不能发现所有操纵、不能证明未来上涨、不能替你交易，也不能把所有链和DEX写成“全市场”。GitHub只证明代码证据，不证明部署、采用或投资价值。</p></article></div><p class="c21-method-ending">本产品只能在已接入范围内自动形成研究线索，不能证明项目靠谱、市场低估、未来上涨或一定能够退出。最终投资判断仍由用户负责。</p>`}
function detail(){const item=data.items.find(x=>x.projectId===params.get("id"));let back="candidate-pool.html?view=all";try{const saved=JSON.parse(sessionStorage.getItem("c21-return")||"null");if(saved?.url)back=saved.url}catch(_){}if(!item){main.innerHTML=`<a class="c21-back" href="${esc(back)}">← 返回</a>${head("项目详情","当前完整快照中没有这个项目","它可能未通过前台门槛、已到第91天退出，或链接来自旧快照。")}`;return}const github=item.productEvidence.github||{};main.innerHTML=`<a class="c21-back" href="${esc(back)}">← 返回原列表</a><header class="c21-detail-head"><span class="c21-kicker">${esc(item.relationshipClass)} · ${esc(item.relationshipLabel)}</span><div class="c21-card-top"><div><h1>${esc(item.canonicalName)} <small>${esc(item.symbol)}</small></h1><p class="c21-lead">${esc(item.displayState.reason)}</p></div>${badge(item)}</div><div class="c21-meta"><span>${esc(item.chainId)}</span><span>发行第 ${item.ageDays} 天</span><span>${esc(item.contractAddressMasked)}</span><span>数据到 ${esc(fmtTime(item.observationHistory.lastSuccessfulAt))}</span></div></header><div class="c21-detail-layout"><div><section class="c21-panel"><h2>为什么能进入前台</h2><div class="c21-checks">${item.hardGate.checks.map(x=>`<div class="c21-check"><div class="c21-check-head"><strong>${esc(x.label)}</strong><span class="c21-status-${esc(x.status)}">${esc(statusLabel[x.status]||x.status)}</span></div><p>${esc(x.reason)}</p><small class="c21-source-note">${esc((x.sourceNames||[]).join("、")||"本项没有可展示来源")} · ${esc(fmtTime(x.observedAt))}</small></div>`).join("")}</div></section><section class="c21-panel c21-section"><h2>四条证据路径</h2><div class="c21-paths">${item.evidencePaths.map(x=>`<div class="c21-path"><div class="c21-check-head"><strong>${esc(x.label)}</strong><span class="c21-status-${esc(x.status)}">${esc(statusLabel[x.status]||x.status)}</span></div><p>${esc(x.plainReason)}</p><div class="c21-metrics">${(x.supportingMetrics||[]).map(m=>`<span class="c21-metric">${esc(m.label)}：${esc(fmtNum(m.value))}${esc(m.unit||"")}</span>`).join("")}</div>${x.unknowns?.length?`<p class="c21-source-note">仍未知：${esc(x.unknowns.join("、"))}</p>`:""}</div>`).join("")}</div></section></div><aside><section class="c21-panel"><h2>当前事实</h2><dl class="c21-facts"><div><dt>T0</dt><dd>${esc(fmtTime(item.t0.value))}</dd></div><div><dt>真实历史</dt><dd>${item.observationHistory.validHistoryDays}/${item.observationHistory.expectedHistoryDays} 天</dd></div><div><dt>产品证据</dt><dd>${esc(item.productEvidence.plainSummary)}</dd></div><div><dt>可信度</dt><dd>${esc(confidenceLabel[item.dataConfidence.level]||item.dataConfidence.plainReason)}</dd></div><div><dt>硬风险</dt><dd>${esc(item.riskSummary.plainReason)}</dd></div></dl></section>${github.status==="qualifying"?`<section class="c21-panel c21-section c21-boundary"><h2>仅代码证据</h2><p>${esc(github.repository||github.organization)}</p><p>${esc(github.boundaryNote)}</p>${github.repositoryUrl?`<a href="${esc(github.repositoryUrl)}">打开仓库</a>`:""}</section>`:""}<section class="c21-panel c21-section"><h2>五因子方向</h2><div class="c21-metrics">${item.factorDirections.map(x=>`<span class="c21-metric">${esc(x.factor)}：${esc(directionLabel[x.direction]||x.direction)}</span>`).join("")}</div><p class="c21-source-note">方向用于排序和变化解释，不单独定义凸性线索。</p></section></aside></div>`}
({home,all,changes,method,detail}[mode]||home)();
})();
