# C1.1 凸性发现质量升级

## 版本目标

把“凸性”任务中已经验证有效的研究方法转成凸性机会中心能够独立运行的发现、筛选、解释和验收能力。任务内容只作为阶段性静态校准样本，不建设一键同步，也不依赖任务持续运行。

## 产品边界

- 本版本只属于凸性机会中心与凸性工作台。
- RWA项目中心的项目、数据、规则、排序、复核和运行状态继续完全独立。
- 公共API资源库只共享连接、认证、额度和健康状态，不共享业务数据。
- 每条支线使用 `C1.1-编号` 命名，反馈时必须先说明 `C1.1` 版本大类，再说明本次支线。

## 九条支线

| 顺序 | 支线 | 目标 | 状态 |
|---|---|---|---|
| C1.1-01 | 公共API资源库 | 统一管理可复用API的连接、认证、额度和健康状态 | 已完成 |
| C1.1-02 | 黄金校准集 | 将任务中的正例、极限边界、观察和排除案例固化为模型基准 | 已完成 |
| C1.1-03 | 四层自动筛选模型 | 依次完成身份与交易、硬事实、凸性结构、行动分层 | 已完成 |
| C1.1-04 | 高价值信源接入 | 接入GitHub、DefiLlama、Snapshot、Cactus等高信息密度来源 | 已完成 |
| C1.1-05 | 发现召回与身份归因 | 扩大海量项目召回，同时保留资产、项目和价值捕获归属边界 | 已完成 |
| C1.1-06 | 发现漏斗 | 展示每层进入、淘汰、待核验数量和逐项目原因 | 已完成 |
| C1.1-07 | 机会中心重排 | 按行动阶段、风险、剩余凸性、点火距离和交易性展示 | 已完成 |
| C1.1-08 | 变化解释 | 明确每次项目升降级由哪条事实、风险或行情变化触发 | 已完成 |
| C1.1-09 | 模型验收 | 使用黄金集和新增盲测案例评估召回、误报、漏报和动作一致性 | 已完成 |

## 当前进度

`C1.1-01` 至 `C1.1-09` 已全部完成。机会中心以四层模型作为研究动作来源、以现行硬门槛作为执行否决层，再按风险、剩余凸性、点火距离、交易性和综合关注分进行组内排序。变化解释为全部项目建立逐次比较基线，记录行动阶段、风险、研究判断、交易性和达到阈值的行情变化，并保留字段、规则和信源依据。全部项目保留，不用固定条数截断。

模型验收完成黄金集 `14/14` 与独立情景盲测 `17/17`，行动召回和精确率均为 `100%`，安全型误报为 `0`。盲测同时修复了“缺少点火与失效窗口仍进入极限试仓”和“价格充分反应仍进入普通建仓”两项阻断级缺陷。

以上只代表规则工程验收通过，不代表已经证明真实投资有效性。下一最高优先级应进入 `C1.2 真实结果闭环与模型校准`，自动保存结论后的价格路径、事实兑现、失效原因和退出结果；累计足够独立案例后再评估真实命中率、收益分布与最大回撤。
