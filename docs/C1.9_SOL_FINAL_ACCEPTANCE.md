# C1.9 Sol 独立终验与发布结论

终验日期：2026-08-03  
终验角色：Sol  
结论：**34 项冻结验收全部通过，C1.9 已发布。**

## 发布内容

- 机会中心成为面向普通用户的前台，只保留“机会首页、全部机会、重要变化、我们如何判断”。
- 项目详情改为从结论、机会卡片和变化卡片进入的上下文页面，不再占用顶级导航；返回会恢复来源、筛选、页码和滚动位置。
- 工作台成为独立后台，以浅色左栏归纳为七个管理栏目；任意后台页面只在左栏底部显示一次当前版本 C1.9。
- 工作台首屏只保留系统状态、待处理项、最近结果和当前任务；更新中心持续显示七阶段、计数、心跳、耗时、失败聚合和单项重试入口。
- 前台后台用语完成分离；科学计数法、后台枚举、任务术语和重复动作前缀不再直接暴露给普通用户。
- 桌面冷启动继续先开放上次有效页面，再在后台重建快照；页面就绪实测 5.43 秒。

## Sol 修复的发布阻断

1. 修正前台残留的机器语言、风险枚举、错误自动跟踪数量、详情返回上下文和筛选记忆。
2. 补全项目详情的判断、变化、催化、风险、失效条件、摘要和证据时间结构。
3. 把后台概览压缩为四个首要信息块，修复浅色七栏目导航和唯一版本号位置。
4. 修正长任务陈旧状态、错误剩余时间、技术型当前活动、失败跨历史累计和跟踪明细无限展开。
5. 把最新一轮失败按来源和原因聚合，只计算本轮 104 项，不把历史 429 项混入当前影响范围。
6. 修正冷启动测试对相对启动命令的误判，并确认桌面壳只属于本项目。
7. 修正详情页 `2.06071e+06` 等科学计数法，改为“206.1万”等普通语言。

## 独立验证结果

- 冻结需求：5 份文件哈希全部匹配；需求集 SHA-256 为 `cd19167c610e0a463378b60834c59cabfb29fc8f06df3a4eb949255c772f68be`。
- Python：58/58 通过；JavaScript 语法和 Python 编译检查通过。
- 路由：28/28 个唯一角色页面返回 200；30/30 个页面与资源可访问。
- SQLite：`integrity_check=ok`，外键异常 0；54 张表，596 个项目、598 个机会案例、21,597 条原始/标准/可溯源事件。
- 业务边界：5 个核心规则文件哈希与开发基线完全一致；评分、动作、L0-L5、风险和交易维度未改。
- 真实点击：普通用户前台完整路径、详情进入/返回、筛选分页记忆、重要变化零状态、维护者工作台与失败聚合路径通过。
- 桌面：启动、单实例、窗口位置保存与恢复通过；默认端口 8766；冷启动页面 5.43 秒就绪，后台重建成功。
- 视觉：14 张最终截图覆盖三种视口、125% 缩放模拟、桌面宿主、前台、详情、工作台和失败状态。
- 独立性：活动运行目录到 RWA、旧整合项目和源项目的禁用引用为 0。

## 当前真实运行风险

2026-08-03 14:00 的自然调度已按时执行，Windows 任务结果为 0，但本轮 104 个到期跟踪项目因所需来源未在本轮运行而全部失败。产品现在会明确显示失败来源、影响 104 项、保留上次有效结论和单项重试入口，不再把它误写成“有数据”或“成功”。这不是 C1.9 前后台体验发布阻断，但仍是下一项最需要处理的运行质量问题。

自动运行目前由用户在更新中心暂停；这是保留的用户设置，不是程序故障。恢复后，计划任务仍会按本项目独立配置运行。

## 发布依据

机器可读完整清单见 `docs/C1.9_ACCEPTANCE_MANIFEST.json`，截图清单见 `docs/C1.9_SCREENSHOT_MANIFEST.json`，阶段状态见 `docs/C1.9_PHASE.json`。
