# 企鹅投研-凸性｜当前状态

更新日期：2026-08-14（Asia/Shanghai）

## 当前版本

- 迁移版本：M1.0
- 数据主干：C1.7 已发布并保持有效
- 当前已发布桌面版本：C2.4 普通用户体验与机会发布重构（继承 C2.3 稳定桌面容器）
- 当前网页业务主干：C2.4 普通用户体验与机会发布重构
- C2.3 阶段：`independent_full_acceptance_complete_released`
- C2.4：已于 2026-08-13 正式冻结、完成整版开发、79/79 独立终验并发布；阶段为 `independent_full_acceptance_complete_released`
- D0 开发体系重置0：已于 2026-08-14 正式冻结并完成实现与开发自测；它不是产品版本，当前阶段为 `implementation_self_test_complete_waiting_independent_acceptance`，用户可见版本继续为 C2.4
- 上一已发布体验版本：C2.1 90天新发项目自动筛选与可解释前台，继续作为冻结回滚基线
- 产品总路线图：`docs/PRODUCT_ROADMAP.md`；已重写为零人工复核的 90 天新发代币项目发现与可信度筛选
- C2.1：用户已于 2026-08-10 明确冻结；整版实现、独立终验、冻结范围修复、调度迁移和发布全部完成
- C2.2：用户已于 2026-08-11 明确冻结并授权整版开发；第二轮独立终验与发布完成；更新中心可控性、现役任务隔离、候选生产化、第一关→凸性跟踪交接及候选库真实首轮跟踪闭环修复均已发布
- C2.3：用户已于 2026-08-12 冻结并授权整版开发；独立终验 38/38 通过，正式桌面入口已切换为 `.NET 10＋WPF＋WebView2` 容器，旧 PowerShell 入口作为隐藏回滚资产保留
- 模型规则：自 2026-08-10 起使用什么模型由用户自行决定；助手可以建议，但不再审核模型或强制提醒 Sol/Luna 切换

## 用户现在能看到什么

### 前台：凸性机会中心

- 顶部只保留“机会首页、全部机会、重要变化、我们如何判断”。
- 首页显示全部通过宽硬门槛的项目，不要求先形成凸性线索。2026-08-12 17:52 修复切点为 8 个：活跃项目 2、持续观察 6、凸性线索 0、等待首轮跟踪 0；这些对象均已完成 T0/新币第一关并通过宽硬门槛。
- 发布不足 14 天只使用已真实发生的历史，不等待、不补零、不因年龄加权；当前 Botcoin 发布第 0 天仍正常可见。
- 全部机会提供产品证据、数据状态、排序、20 条分页和详情返回；重要变化按同项目同一时点分组。
- 项目详情不再是顶级栏目。从首页、全部机会或重要变化点击项目进入，返回时恢复原来的页面上下文。
- 前台不显示版本号、任务 ID、Watcher、游标、调度、重试、日志或模型术语；科学计数法和后台枚举已转换为普通语言。
- 本地桌面版右上角保留低干扰“管理工作台”入口；普通用户不进入后台也能完成全部阅读路径。

### 后台：凸性工作台

- 使用浅色左侧导航，管理页面归入七个栏目：工作台概览、更新中心、项目管理、证据与来源、监控与催化、模型与规则、系统设置与日志。
- 当前桌面版本只在后台左栏底部显示一次“当前版本 C2.3”；C2.2 只作为继承的网页业务主干名称保留，不再冒充当前桌面版本。
- 更新中心已拆成“90天新币筛选”和“凸性跟踪更新”两个独立页面；两页各自显示手动更新、仅手动/自动频率、暂停和实时进度，设置、来源、状态与脚本入口不再混用。
- 两页均提供立即更新、仅手动/每小时/每3小时/每6小时/每12小时/每天、暂停自动、暂停当前任务；只有额度受限或来源连接失败提供对应作业内的单来源更新。
- 手动与自动共用同一条隐藏、单实例、可恢复流水线；页面显示当前阶段、完成时间和正式任务迁移状态。
- 凸性跟踪页把旧工作台任务放入“历史主干维护（不影响 C2.2 前台）”；历史失败默认折叠、不计入现役故障，只在维护旧版页面或回滚能力时手动展开。
- 更新中心显示七阶段进度、处理计数、运行时间、最近心跳、剩余时间可信度和当前活动；失败先按来源与原因聚合，再展开 20 条/页明细和单项重试入口。
- 运行、暂停、恢复、立即更新和重试只存在后台，并明确作用范围。

## 数据、恢复与独立性

- C2.1/C2.2 采集库 `data/c2.1-pipeline.db` 在本次修复前为 8,251,932,672 字节，候选记录 4,591,622、生产记录 2,803,897；正式候选生产仍在持续写入，这些数字会继续变化。本次真实首轮跟踪完成 7 个前台项目。
- 两库均 `quick_check=ok`；主库当前为 624 个项目、626 个案例且本次未写入。采集库只新增 `candidate_tracking_records` 首轮跟踪断点表；迁移前备份为 `backups/c2.2-tracking-repair/c2.1-pipeline-before-tracking-repair-20260812T1530.db`，大小与源库一致且 `quick_check=ok`。
- 2026-08-11 真实桌面快捷方式验收证明2026-08-10的启动只读修复不完整：旧更新守护恢复仍会写主库。现已改为启动只读打开主库并只校验C2.1原子快照；最终冷启动及三项桌面验收前后主库 SHA-256 均为 `8cb2bce2f934ed7697cf62c28171077793dd7148b138e5ec8e164f42727a0fa2`。已存审计记录保留，未删除或回滚。
- C2.0 历史发布快照为 600 个项目、602 个机会案例；Gate 0 早期只读复核为 606/608。这些是历史节点，不冒充当前实时数量。
- 现场只读复核原始事件、标准事件、带定位与内容哈希的可溯源事件均为 29,382 条；跟踪执行历史 656 条，数据主干积压 0。
- SQLite：`integrity_check=ok`，外键异常 0。
- Gate 0 终验时的历史数据库节点为 402,776,064 字节，SHA-256 `f8000b94b88079a6e4749a48e9d48c90d66bd169204a89a5ac1a94a560b578e0`；该节点只用于证明 Gate 0 当时未写库，不冒充当前实时哈希。
- C1.9 开发前备份：`backups/c1.9-pre-implementation-20260803T123722/convexity.db`。
- Sol 修复前备份：`backups/c1.9-sol-pre-fix-20260803T134314/convexity.db`。
- C2.0 Sol 修复前备份：`backups/c2.0-sol-pre-fix-20260803T100254191Z/convexity.db`。
- M1.0 的 585/585 与 C1.7-00 的 589/590 继续作为不可变历史基线保留，不冒充当前实时数量。
- 活动运行目录到 RWA、旧整合项目和源项目的禁用运行时引用为 0；端口、数据库、状态、日志、缓存、锁、任务和窗口记忆均属于本项目。
- 5 个核心研究规则文件与 C2.0 需求锁哈希完全一致；评分、动作、L0-L5、风险、剩余凸性、交易性、点火距离和生命周期规则未改。

## C2.3 独立最终验收

- C2.3 冻结验收 38/38、.NET 测试 8/8、实时页面/资源 32/32 通过；未改动的 `app/` 和 `desktop/` 树哈希与实现前基线一致。
- 真实桌面覆盖冷/热启动、单实例、端口冲突、产品身份失败、WebView2 缺失修复页、页面渲染进程一次自动恢复、1180×760、1440×900 和最大化视口。
- 正式桌面快捷方式已从旧 PowerShell＋Edge 容器切换到 `desktop-host/publish/win-x64/PenguinConvexity.Desktop.exe`；只恢复快捷方式的回滚演练通过后已重新发布 C2.3。
- 主库和采集库均 `integrity_check=ok`、外键异常 0；主库 624/626，候选生产 PID 4160 和 runId 未变，终验收口时已扫描 2,458,897。
- 完整证据：`docs/C2.3_ACCEPTANCE_MANIFEST.json`、`docs/C2.3_FINAL_ACCEPTANCE.md` 和 `docs/C2.3_RELEASE_MANIFEST.json`。

### C2.3 发布后：来源单项更新反馈与版本显示维护

- 修复“只更新这个来源”点击后当前卡片没有任何反馈：卡片现在会直接显示正在提交、已受理、已有任务运行或明确失败原因，不再要求用户回到页面顶部猜测是否生效。
- 单项更新继续使用既有隐藏、单实例、可恢复入口；只传递当前作业与当前来源，不改变筛选规则、阈值、自动频率、数据库结构或候选历史断点。
- 规则重算阶段新增周期心跳，避免后台仍在正常计算时被页面误报为“可能中断”。
- 后台左栏当前版本已从遗留的 C2.2 修正为 C2.3；C2.3 桌面容器与 C2.2 网页业务主干的继承关系不变。
- 精确变更、哈希和受影响验收见 `docs/C2.3_POST_RELEASE_MAINTENANCE_20260812.json`。

## C2.2 第二轮独立最终验收

- C2.2 冻结验收 39/39、全量 Python 73/73、C2.2 核心与恢复测试 27/27、C2.1 继承验收 7/7、实时页面/资源 32/32 通过。
- 第二轮修复三个发布级问题：C2.2 快照与旧渲染器载入顺序、桌面启动器旧版本误判、Windows 进程锁误终止风险；受影响测试和完整回归均已重跑。
- 真实桌面快捷方式冷启动、首页业务内容、重复启动单实例、关闭重开窗口恢复通过；启动前后两库哈希不变。
- 主库 SHA-256 为 `d301e24f9bf0a4d589a1d7a0ce4a52336aa2a6aaf458d60017e8b9e7a5816382`，采集库 SHA-256 为 `71adf4864e57d5fd00164f0dc0894a2aa1faf904687bbdd9aaa673eee4290327`，均与实现前基线完全一致。
- 正式调度继续只有 `PenguinConvexity-C1.8-Scheduler` 一个启用的长期任务；两个作业均保持自动、每 24 小时、未暂停，隐藏运行黑框 0。
- 发布备份：`backups/c2.2-release-20260811T1544/manifest.json`。完整证据：`docs/C2.2_ACCEPTANCE_MANIFEST.json`、`docs/C2.2_FINAL_ACCEPTANCE.md`、`docs/C2.2_RELEASE_MANIFEST.json`。

## C2.2 更新中心发布后修复

- 两页手动更新、独立频率、暂停和实时进度均已恢复；来源按新币筛选、凸性跟踪和共享上游明确归属。
- 当前新币筛选需要处理 0 条、能力边界 103 条；凸性跟踪来源需要处理 0 条、能力边界 20 条。无数据和不支持不是后台错误。
- 当前另有 1 个既有工作台任务待恢复：“机器状态与结论发布”。异常数据类型防崩溃已修复，但验收遵守只读边界，没有替用户点击重试。
- 测试污染正式运行状态的问题已修复；82/82 个测试文件完成后，主库、采集库和正式运行状态哈希均不变。
- 当前主库 SHA-256 为 `a7b68e3eb341df98fbb9803c7565a8776f63813bfd206ae0a3f3711da5e7727b`，采集库 SHA-256 为 `71adf4864e57d5fd00164f0dc0894a2aa1faf904687bbdd9aaa673eee4290327`。
- 4项 Edge 页面验收全部通过且未发出 POST；桌面快捷方式与单实例通过。桌面控制助手返回 `Transport closed`，因此没有用盲点操作伪造点击结论。

### 现役任务隔离与后台解耦

- C2.2 现役作业固定为“90天新币筛选”和“凸性跟踪更新”；旧“机器状态与结论发布”继续保留，但只属于历史主干维护，不再要求用户重试以恢复 C2.2 前台。
- 凸性跟踪已从旧 `full_refresh` 解耦，改用 `c2_2_convexity_tracking_refresh`；只运行证据健康、持续证据、市场与退出、数据主干和到期跟踪，不自动运行旧机器评分、机器结论、催化发布、项目发现或身份复核。
- C2.2 组件进度只写自身作业状态；旧 `/api/update-status` 只用于历史维护区，不再控制现役进度条或红色故障提示。
- 主库副本真实运行约 111 秒成功，最新运行的机器结论来源记录为 0；正式数据库、采集库和正式运行状态未被副本验收改写。
- 两种桌面视口 4/4 真实渲染通过：历史失败默认折叠，现役凸性跟踪显示完成，无横向溢出、控制台错误、失败请求和 POST。正式快捷方式重启后健康状态为 C2.2，专属任务已载入，重复启动仍只有一个窗口。

## C2.1 独立最终验收

- 2026-08-11 更新中心页面拆分维护验收：两个独立页面双向跳转及脚本边界通过，本地真实渲染无控制台错误；当前页面/资源 32/32、完整 Python 测试文件 76/76 通过。未点击更新按钮，主库 SHA-256 仍为 `8cb2bce2f934ed7697cf62c28171077793dd7148b138e5ec8e164f42727a0fa2`。
- 冻结需求 8/8 哈希一致，需求集 SHA-256 为 `f8784fb3ee03df42d56171f97dfb37707107f50fcbceba4647764a98a16b6696`。
- C2.1 正式验收 58/58、Python 测试文件 75/75、页面/资源 31/31 通过；JavaScript 语法和 Python 编译通过。
- 2026-08-11 原桌面入口结论因真实快捷方式弹窗被撤回并重做。启动器 C2.1 版本判断、空日志 Null 处理和完整启动只读边界均已修复。
- 纠正验收从正式 `.lnk` 完成真实冷启动、首页内容、重复双击单实例、关闭重开窗口恢复和主库哈希不变；受影响回归及完整 75/75 再次通过。
- 快照构建 ID `c21-20260810T131245Z-2831d7`，宽硬门槛 12/12 全部前台可见；五状态互斥且合计一致。
- 真实点击验收覆盖首页、全部机会筛选、详情返回、重要变化分组、判断边界和更新中心“每天”模式。
- 12 张桌面验收图已生成并视觉检查，1180×760 与 1440×900 无整页水平溢出；未开发移动端。
- 正式调度仍使用唯一任务 `PenguinConvexity-C1.8-Scheduler`，15 分钟只检查到期/恢复，新一轮频率为每 24 小时，隐藏启动黑框 0。
- 完整证据：`docs/C2.1_ACCEPTANCE_MANIFEST.json`、`docs/C2.1_FINAL_ACCEPTANCE.md`、`docs/C2.1_SCREENSHOT_MANIFEST.json`、`docs/C2.1_RELEASE_MANIFEST.json`。

## C2.0 历史最终验收

- 冻结需求：5/5 正式文档和 5/5 核心研究文件哈希一致；需求集 SHA-256 为 `f90bca4ff8764290fe5103e6191c6c1b0bd54126b94a03f708cb83cc20b3761b`。
- C2.0 验收：A01-A09、B01-B09、C01-C09、D01-D09、E01-E06、F01-F08、G01-G11，共 61 项全部通过。
- 全量 Python：59/59 通过；JavaScript 语法和 Python 编译检查通过。
- 唯一角色路由：29/29，其中前台 2、上下文详情 1、桌面宿主 1、后台 25；31/31 本地页面或资源可访问。
- 双快照：构建 ID `c2.0-b9ec727d9d1947f1`，项目 600、案例 602、首页有效信号 0/5、变化链 1,447、闭环队列 2,981；原子失败保护通过。
- 真实点击：零状态、全部机会筛选与排序理由、整卡点击与键盘导航、详情进入/返回、重要变化折叠/展开、判断边界、质量漏斗/队列和返回滚动恢复全部通过。
- 桌面：启动、单实例、窗口记忆通过；发布版冷启动 5.41 秒，后台重建成功；健康接口返回 C2.0。
- 调度：隐藏 Windows 任务隔离验收通过，真实任务为 Ready；用户暂停设置保持不变。
- 视觉：17 类场景全部检查，并重新生成 11 张当前真实页面及四种视口截图。
- 完整证据：`docs/C2.0_ACCEPTANCE_MANIFEST.json`、`docs/C2.0_SOL_FINAL_ACCEPTANCE.md`、`docs/C2.0_SOL_SCREENSHOT_MANIFEST.json`。

## Gate 0 Sol 最终验收

- 正式运行 `gate0-solfinal-20260809T045924Z-f7bbd2` 已覆盖固定 90 天窗口与六链冻结协议范围：262/262 个分片、5,922,807 条创建事件、4,585,955 个唯一候选。
- 请求账本 6,723 条：成功 6,477、来源失败 246；`no_data`、额度受限、不支持、配置缺失和程序失败在本轮请求中均为 0，但六类状态仍分别保留并有自动测试。
- 独立重算检查了 262 个分片哈希和全部事件/候选：重复、解码失败、缺池、缺代币、缺时间、越界、未来时间、候选最早事件差异均为 0。
- 完成后真实再次触发 Windows 任务，1 秒内幂等退出；网络请求未增加，候选、账本、摘要、验证和分片清单哈希不变。
- Ethereum 4、Base 8、Arbitrum 6 个未确认标签继续标记为 `unsupported`；结果不是全市场完整母池，也不能单独定义全局 T0。
- 官方结果已提升到 `runtime/gate0-shadow/backfill/latest.json`；旧 latest 指针备份为 `latest.pre-gate0-sol-promotion-20260809.json`。
- 一次性 `PenguinConvexity-Gate0-Backfill` 恢复任务已在放行后禁用，避免每 15 分钟无意义唤醒；任务定义保留且可恢复，C1.8 产品调度不受影响。
- 进度页和数据报告已重建；C2.0 产品代码、冻结文档、核心规则、C1.8 调度和用户暂停设置均未改变。
- 完整证据：`docs/GATE0_ACCEPTANCE_MANIFEST.json`、`docs/GATE0_SOL_FINAL_ACCEPTANCE.md`、`reports/gate0-backfill-progress/report.html`、`reports/gate0-data-preflight/report.html`。

## 当前真实运行状态与风险

- 当前 11 个前台项目全部为 C 类“项目关系未确认”，其中 8 个只有代码证据、3 个有结构化业务数据。
- 当前凸性线索为 0；这是冻结四路径和防抖的真实结果，不是空页。程序不用综合分或降低门槛伪造线索。
- 4,590,214 是冻结 DEX 范围的代币/池候选记录，不是已确认项目数，也不是全市场完整母池或全局 T0。
- C2.2 本页来源当前均无需要处理项：新币筛选能力边界 103 条、凸性跟踪能力边界 20 条；边界仅表示无数据或当前不支持。旧工作台另有 1 条历史失败记录，仅供历史维护，不与现役来源健康混算。
- 正式任务保持自动开启、未暂停、新周期每 24 小时一次。2026-08-11 10:15 已自动识别一轮中断手动更新并以 `resume` 恢复；没有新建任务或启动第二个写入者。
- 计划任务使用当前 Windows 用户登录令牌：关闭产品或 Codex 后已启动任务继续；关机/休眠时停止，下次 Windows 登录后从已提交游标恢复。

## 下一最高优先级

C2.2 现役任务隔离、后台解耦和候选生产化覆盖修复均已完成并通过独立终验。当前无需用户点击“机器状态与结论发布”，也不要为了清除历史红字运行旧整套全量更新。

用户已于 2026-08-11 20:54 明确授权正式处理 4,590,214 条历史候选。隐藏后台进程已启动；920 个历史分片覆盖 Gate 0 已验收的 4,585,955 条候选，6 个日常分片覆盖其余 4,259 条增量候选，合计与总分母一致。

当前最高优先级：C2.4 已发布，当前无需用户操作。历史候选生产已于 2026-08-13 17:52 保存全部断点并进入 `ready`，后续只需观察日常增量和凸性跟踪正常更新；不要为发布重复点击或重跑 Gate 0/459 万历史扫描。

任何超出 C2.4 需求锁的新门槛、新算法、新页面或删除已有功能仍需用户另行冻结；不得借发布后维护修改已冻结规则。

## C2.2 候选生产化覆盖修复发布

- 修复需求集 SHA-256：`184630e821918881b0306ce8078e86dcc2b27c78d515aad7b0bbb52e8aea4a03`；原 C2.2 需求集继续为 `e689e0177a83918e5075c03f524ab9220ae070d193dad7c580ca35ea91c87770`，两套冻结文件均匹配。
- 独立固定样本 10/10、候选专项回归 12/12、全量非发布 Python 测试文件 83/83、桌面页面 4/4 通过。
- 独立终验修复缺失交易笔数语义：未知数据保持 `null` 和 `source_pending/no_data`，不再误写成“等待交易形成”。
- 正式桌面快捷方式完成冷启动、重复启动单实例、窗口状态保存和关闭后恢复；唯一长期调度仍为 `PenguinConvexity-C1.8-Scheduler`，没有新增黑框或第二调度器。
- 采集库迁移前备份：`backups/c2.1-pipeline-pre-candidate-production-20260811T200500.db`；迁移只建立生产化结构，没有生成正式候选结果。
- 用户于 2026-08-11 20:54 明确授权后正式启动。启动监测发现分片成员缺少 `candidate_id` 索引，已在历史结果写入前安全停止，补充索引并通过 14/14 专项测试后恢复；查询计划已从全表扫描改为索引查找。
- 21:00 首个日常分片和首个 5,000 条历史分片均已原子完成，后台按日常—历史队列交替继续；正式主库、Gate 0 和唯一 Windows 调度器未改动。
- 唯一 Windows 调度器现会在登录后或每 15 分钟唤醒时检查：若历史扫描已授权、未完成、未暂停且进程不在，则从现有分片和游标自动重新拉起；不会创建第二个任务或重复已完成分片。
- 完整证据：`docs/C2.2_CANDIDATE_PRODUCTION_REPAIR_ACCEPTANCE_MANIFEST.json`、`docs/C2.2_CANDIDATE_PRODUCTION_REPAIR_INDEPENDENT_ACCEPTANCE.md`、`docs/C2.2_RELEASE_MANIFEST.json`。

## 2026-08-12 候选并发、断点与可见性维护

- 桌面启动器不再把耗时的 `/api/c2.2/status` 当启动健康检查；正式健康检查 0.46 秒通过，真实桌面启动、单实例、保存与恢复通过。
- 候选状态改读工作进程原子发布的缓存并仅叠加小型分片表；接口由约 6 秒降至约 0.08 秒，不再以大范围只读事务阻塞扫描写入。
- 历史扫描和日常筛选现在严格串行写 `data/c2.1-pipeline.db`：日常筛选先请求历史进程到原子断点安全暂停，发布后从同一断点恢复；数据库锁失败的错误记录也会先回滚再写失败范围，避免二次锁异常。
- 恢复不再重复准备既有 920 个历史分片；历史进程只按小时检查日常增量，不再每完成一个 5,000 条分片就全库重查。资格交接只评估新完成且尚未评估的对象，不重复全量规则计算。
- 真实筛选周期已完成并发布：当前采集库 4,591,622 条候选、2,173,467 条生产记录、487 个资格批次、12,810 条当前评估；前台为 9 个真实项目，凸性线索 0、活跃项目 3、观察中 6。其中 6 个来自历史候选生产批次，另外 3 个来自既有日常筛选资产。
- 历史原料不会直接出现在机会前台。当前 12,747 条达到后台凸性跟踪资格，其中 12,736 条为 D 类、12,699 条身份待确认；只有身份、产品证据和 A/B/C 关系满足前台合同者公开。更新中心已增加 D 类后台数量和这条边界说明。
- 两库 `integrity_check=ok`、外键异常 0；候选专项 19/19、独立样本 10/10、C2.2 运行 10/10、快照 4/4、验收 8/8 通过，JavaScript 语法与真实桌面集成通过。

## 2026-08-12 第一关→凸性跟踪交接维护

> 本节是已经撤销的中间结论。当时把约 1.42 万条后台资格记录误称为凸性跟踪输入，违反了“先过新币第一关”的正式链路。当前结论以下方“C2.2 T0—新币第一关正式交接纠错发布”为准。

- 已确认这是 C2.2 遗留缺陷，不属于 C2.4；C2.4 讨论在修复期间保持暂停，未修改任何筛选门槛、四条强路径或产品分类。
- 修复前，C2.2 跟踪快照错误地把第一关公开 A/B/C 前台项目当作全部跟踪输入，并要求前台集合与后台集合完全相等；因此历史生产已形成的约 1.42 万条资格记录没有正确呈现在凸性跟踪作业和后台统计中。
- 现在凸性跟踪从“已完成资格批次”读取全部后台资格，使用生产记录中的真实 T0；D 类和身份未确认对象只进入后台数据库，不生成公开项目详情。前台仍只显示满足合同的 A/B/C 项目。
- 手动与自动凸性跟踪每次最多交接 500 条待首轮对象，先等待历史扫描到达安全断点，再使用同一单实例数据库写入；失败可从剩余待处理集合继续，不会重头计算 1.42 万条。
- 发布快照记录后台跟踪候选 14,425、已有首轮结果 14,079、等待首轮 346、公开详细对象 9；工作台概览和凸性跟踪页均已在正式 C2.3 桌面容器中真实显示。
- 全量详细后台对象曾在临时目录测得会生成两个约 175MB 页面文件，因此未发布；修复后正式跟踪快照约 0.41MB、后台快照约 0.85MB，真实构建约 2.28 秒。
- 采集库只读 `quick_check=ok`、外键异常 0；没有修改数据库结构、正式数据、历史扫描断点、唯一 Windows 调度器或 C2.3 桌面容器。
- 发布证据：`docs/C2.2_TRACKING_HANDOFF_MAINTENANCE_20260812.json`。

## C2.3 稳定桌面容器与浏览器隔离

- 用户已于 2026-08-12 明确回复“冻结吧”，C2.3 正式冻结；需求集 SHA-256 为 `82057a667611285c8486008145ac105fa7549f4e56eeb9dcfcf68b9333008746`。
- 用户随后明确授权“开始C2.3整版开发”。当前阶段为 `implemented_waiting_independent_acceptance`：整版开发与开发自测完成，尚未独立终验和发布，正式桌面快捷方式未切换。
- C2.3 只批准把 PowerShell 调起普通 Edge 的桌面入口替换为 `.NET 10 LTS＋WPF＋WebView2 Evergreen`；C2.2 前后台、页面、规则、两库、快照、更新设置和后台任务全部零删除继承。
- 桌面稳定性优先于未来网页版复用；未来网页版单独立项。现有本地 HTTP 业务通道因当前风险最低而保留，C# 不形成第二套业务取数或数据库写入通道。
- 测试按实际风险分层：开发中只跑受影响测试，未修改业务资产以冻结哈希、契约和快速冒烟证明继承；只有共享核心、规则、数据库、调度或影响边界不明时才扩大为全量回归。
- `.NET 10.0.400` 安装在 `F:\DevTools\dotnet`；Windows x64 自包含容器已生成。开发验收为 .NET 8/8、现役页面/资源 32/32，真实窗口 710 毫秒、页面 982 毫秒、重复唤醒 127 毫秒。
- 真实只读页面检查覆盖前台、详情返回、工作台和两个独立更新页面；没有点击更新、暂停或设置。`app` 与 `desktop` 业务树哈希均与实现前基线一致，主库 `quick_check=ok`、外键异常 0。
- 候选生产 PID 4160 和 runId `candidate-production-20260812T015104Z-96c732` 保持连续，扫描进度从 2,353,897 增至 2,403,897；8766 服务 PID 20844 和唯一调度器保持不变。
- 完整开发证据：`docs/C2.3_IMPLEMENTATION_BASELINE.json`、`docs/C2.3_IMPLEMENTATION_STATUS.json`、`docs/C2.3_IMPLEMENTATION_REPORT.md`。下一步只允许独立终验；通过前不得切换正式入口或标记发布。

## 2026-08-12 C2.2 核心筛选—凸性跟踪链路纠错发布

- 已按用户确认的唯一主链修复：历史市场候选必须先完成 T0、身份、A/B/C/D、产品证据和宽硬门槛核验；只有通过的 A/B/C 才能进入凸性跟踪。
- C2.1 和 C2.2 已统一使用候选生产记录中的稳定 `assetId`；没有候选生产记录的旧评估不再绕过第一关进入快照。
- 现场对账为第一关通过 8、C2.1 前台资格 8、C2.2 凸性跟踪输入 8、已完成首轮跟踪 8、等待首轮 0；D 类或不合格对象误入第二关 0、重复资产 ID 0。
- 前台真实状态为凸性线索 0、活跃项目 2、持续观察 6、等待首轮 0。该结果没有通过降门槛或修改评分伪造。
- 固定测试优先级已经写入 `docs/TEST_PRIORITY_POLICY.json`：核心业务链 → 数据与数量合同 → 真实桌面主路径 → 边缘功能；高优先级失败必须停止，低优先级测试数量不能抵消核心失败。
- 本次分层验收为核心 26/26、数据合同 15/15，合计 41/41；C2.3 正式桌面单实例和核心页面通过；采集库 `integrity_check=ok`、外键异常 0。
- 未修改筛选阈值、贝叶斯规则、四条强证据路径、凸性线索规则或 C2.3 容器；未开始 C2.4，未重跑 Gate 0 或历史母池。
- 修复前备份：`backups/c2.1-pipeline-pre-core-first-gate-fix-20260812T165017.db`，SHA-256 `e7086b2a4b966841ee720c4250b1aed27da6cca53297dc1783bee0f0447a2b5d`。
- 完整发布记录：`docs/C2.2_CORE_PIPELINE_REPAIR_20260812.md`。历史扫描从已有断点继续，当前无需用户操作。

## 2026-08-12 C2.2 T0—新币第一关正式交接纠错发布

- 用户指出上一轮修复仍未完成历史底座到 T0/新币第一关的正式交接，该判断成立；上一轮“交接已完成”的结论已撤销。
- 当前唯一链路固定为：历史底座或日常新增 → 市场确认 → 同步真实 T0 → 显式第一关队列 → 身份、A/B/C/D、产品证据、风险和宽硬门槛 → 只有通过的 A/B/C 进入凸性跟踪与前台。
- 发布前切点：已核验 T0 17,291；正式进入第一关 15,183，其中历史底座 11,575、日常新增 3,608。恢复后台后 779 条积压已全部从断点完成，当前第一关完成 15,183、待处理 0、失败 0。
- 当前通过宽硬门槛 8、前台 8、凸性跟踪正式输入 8；市场确认漏交接、前台绕过第一关、前台绕过硬门槛、D 类误入前台、凸性跟踪绕过第一关均为 0。
- 更新中心已分开显示 T0 总数、第一关进入/完成/待处理、硬门槛通过和前台数量，不再用最终通过数掩盖中间交接状态。
- 核心业务链 27/27、数据合同 15/15，合计 42/42 通过；采集库 `integrity_check=ok`、外键异常 0。
- 本次只在独立采集库增加可恢复第一关队列，没有修改门槛、贝叶斯、凸性算法、主库、C2.3 桌面容器、Gate 0 或调度器。
- 修复前备份：`backups/c2.1-pipeline-pre-t0-handoff-fix-20260812T172952.db`，SHA-256 `fabe406aafa2768d1264a1342d89a67133f356c4ea784589175129413fe92838`。
- 完整发布记录：`docs/C2.2_T0_FIRST_GATE_HANDOFF_REPAIR_20260812.md`。
- 唯一隐藏后台 PID 5356 已自动回到既有历史分片断点继续运行；没有重跑 Gate 0 或已完成分片，当前无需用户操作。
- 18:10 真实页面只读渲染时后台又完成一个历史分片，页面实时增长为 T0 17,304、进入并完成第一关 15,196、历史底座 11,588、日常新增 3,608、待处理 0；漏斗关系正确且没有浏览器错误或写请求。

## 2026-08-12 C2.2 第一关完整性修复

- 现场确认还有两个核心程序缺陷：市场已确认对象未从 `unknown` 进入候选资产核验，新形成的合格产品证据也不会把 D 正确升为 C；两项均已修复。
- 正式库已补齐 11,999 条候选资产内部状态；它们没有被直接放行，仍必须完成身份、产品证据、风险和宽硬门槛。
- 第一关队列 15,794，已完成 15,693，101 条合法等待原子资格批次，无理由卡住 0。
- 第一关通过、C2.1 前台、C2.2 前台和凸性跟踪输入均为 12；前台为活跃项目 11、持续观察 1，凸性线索仍为真实 0。
- 15,561 条 90 天内完成对象中只有 15 条形成“A/B/C 关系＋身份一致＋合格产品证据”交集；当前主瓶颈是身份与产品证据政策，不是 P60、成交额或流动性数值。
- 评估恢复索引补齐后，15,693 条本地重算只用 17.8 秒；不调用外部接口，不重跑 Gate 0 或历史母池。
- 正式库 `quick_check=ok`、外键异常 0；核心相关 50/50 通过，真实桌面冷启动、页面、单实例和 Chrome 隔离通过。
- 完整报告：`docs/C2.2_FIRST_GATE_CORRECTNESS_REPAIR_20260812.md`；漏斗数据：`reports/c2.2-first-gate-correctness/funnel-diagnostic.json`；发布记录：`docs/C2.2_FIRST_GATE_CORRECTNESS_REPAIR_20260812.json`。
- 修复完成后历史任务已从原分片游标恢复，没有从头重跑；原停留分片完成，历史完成分片由 651 增至 652，隐藏后台继续运行。
- 三份中间验收数据库副本合计约 24.1 GiB，当前环境安全策略拒绝删除；它们不参与产品运行，正式回滚备份仍单独保留。

## 2026-08-13 C2.3 外部链接桌面维护

- 已确认截图中的“github.com 拒绝连接”是桌面容器缺陷：外部链接来自 `desktop/index.html` 的内层页面框架，已发布代码只处理顶层导航和新窗口，漏掉了框架导航。
- 现在站内链接继续留在企鹅投研；用户点击 GitHub 等外部 HTTP/HTTPS 链接时交给 Windows 默认浏览器；脚本自动跳转外站仍被阻止。
- 真实桌面验收使用 World Of Claudecraft 的“打开仓库”：Chrome 正确打开 `levy-street/world-of-claudecraft`，企鹅投研仍停留在原项目详情，“返回原列表”和机会首页均可继续使用。
- .NET 定点测试 12/12，通过构建且 0 警告、0 错误；C2.3 八份需求文件和三项继承依赖哈希全部匹配。
- 发布过程中只精确关闭并重开桌面容器；候选生产 PID 39436、runId `candidate-production-20260812T141859Z-9b5b0a` 保持连续，数据库、筛选规则、调度器和网页业务文件未修改。
- 完整记录：`docs/C2.3_EXTERNAL_LINK_MAINTENANCE_20260813.json`。当前无需用户操作。

## 2026-08-13 C2.4 正式需求冻结

- 用户明确回复“可以按冻结稿冻结C2.4”；十份正式范围文件和需求锁已经生成，需求集 SHA-256 为 `411f084d726cfb599536169ec0dcd9fc2d10d6f15e92f0df821385d2d2dbddc0`。
- 唯一主链冻结为“历史底座或日常新增 → 90 天候选初筛 → 凸性深度跟踪 → 机会中心公开展示”；第一关只用四项宽条件，机会中心只读第二关完整公开快照。
- 首页每条链合计最多 10 个只是展示切片；所有公开合格项目继续跟踪并在“全部机会”20 条分页中可见，同链第 11 名是强制验收夹具。
- 第 91 天只迁移曾在 0—90 天完成两关并达到公开条件的同一 `assetId`；不建立普通老项目发现器。
- 29 个既有路由全部登记，批准删除 0；旧功能必须适配、迁移或明确保留未启用，不能静默消失。
- 测试策略改为先从底层数据独立重算核心集合，再验规则、恢复、真实桌面和边缘页面；Tier 0/Tier 1 失败立即阻断，不能用测试时长或低优先级通过数抵消。
- 本次只修改规划、冻结和状态文档；没有修改产品代码、数据库、规则实现、后台进程、调度器、快捷方式或用户设置。当前等待用户另行授权开发。

## 2026-08-13 C2.4 独立终验

- 独立终验最终 79/79 通过，Tier 0、Tier 1 失败均为 0；这是“发现阻断缺陷、定点修复、重新验收后通过”，不是原实现一次通过。
- 修复四条强证据路径未真正使用动态比较组的问题；现按同链同日龄近 30 天、六链同日龄近 30 天、固定后备值三级取样，真实 0 不再被误判为缺失。
- 修复官网脚本依赖链接和 GitHub 个人主页被误认成项目官方仓库的问题；复核 38 个官网映射，撤回 15 个错误映射和 2 个受影响公开项目，并保留逐行恢复备份。
- 统一截止时间为 2026-08-13 17:14:13：底层候选 4,592,870、第一关历史通过 21,062、当前队列 21,048、凸性跟踪 21,048、公开项目 1；11 组集合差异、重复 `assetId`、状态不一致和比较组不一致均为 0。
- Python 定点测试 98/98、真实服务补测 2/2、29/29 路由、JavaScript 语法、两库 `quick_check` 与真实 WPF 冷启动/单实例/窗口恢复/外部链接/双视口均通过。
- 候选生产 PID 39824、页面服务 PID 37108 未因桌面验收重启；没有点击更新按钮，没有重跑 Gate 0 或 459 万历史扫描。
- 验收证据为 `docs/C2.4_ACCEPTANCE_MANIFEST.json`、`docs/C2.4_FINAL_ACCEPTANCE.md` 和 `reports/c2.4-independent-acceptance/core-audit.json`。

## 2026-08-13 C2.4 正式发布

- 用户明确回复“发布 C2.4”；阶段已更新为 `independent_full_acceptance_complete_released`。
- 正式桌面快捷方式继续指向 C2.3 已发布的 `.NET 10＋WPF＋WebView2` 容器，容器内加载 C2.4 页面、规则与四份验收快照；冷启动和重复启动单实例通过，健康接口返回 C2.4。
- 发布前全部验收候选哈希再次匹配，漂移 0；两库只读 `quick_check=ok`、外键异常 0，29 条路由和两个独立作业保持。
- 历史候选生产本轮已正常完成：4,592,870/4,592,870，剩余本地候选 0，runId `candidate-production-20260813T081837Z-bcf2ef`；发布没有重启后台或触发新的扫描。
- 当前没有隔离的测试环境和正式环境。终验候选与正式桌面入口共用同一项目目录；本次发布是封存哈希、更新正式状态并确认入口，不是上传云端、应用商店或复制到另一套服务器。
- 发布收口修复旧健康检查仍验证 C2.2 的遗留问题；现已真实返回 C2.4 就绪且保持页面服务 PID 37108 不变，没有为检查重启服务。
- 18:00 日常调度正常生成发布后的第一组运行快照；新截止时间 18:02:08，第一关当前队列与凸性跟踪均为 21,311、公开项目 1。独立只读重算再次通过，所有核心集合差异、状态不一致和动态比较组不一致均为 0。运行快照哈希变化属于正常数据更新，不是代码漂移。
- 已建立 0.90 MiB 的 C2.4 轻量代码发布备份，共 49 个代码、规则、页面、测试和冻结/验收文件；没有复制 10GB 数据库、运行快照或桌面二进制。恢复仍须用户明确授权。
- 完整发布证据：`docs/C2.4_RELEASE_MANIFEST.json`。

## 2026-08-13 C2.4 Robinhood 与 Solana 核心链路修复

- 已确认 Robinhood 为 0、Solana 只有 1 不是发现层真实结果：当前第一关分别为 3,207 和 10,393，问题发生在首轮基础跟踪、旧主干插队和首页排名。
- 全池结构与历史供应已从 21,235 条首轮基础队列拆出；队列未完成时先发布快照并返回，旧主干不再插队，同一自然日也不再重复执行。
- 少量来源失败只保留单项重试并继续下一批；只有整批全部失败才停止，避免每 15 分钟只推进一小批。
- 修复首页错误要求五因子全部有实测的问题；现在五因子后验可复算且至少有一个真实指标即可按同链贝叶斯结果排序，零实测对象仍不制造假排名。
- 最新真实续跑已发布原子快照 `c24-public-7a8055698135dd41`：公开 51，Robinhood 30、Solana 19、Ethereum 1、Base 1；首页 22，集合差集均为 0。
- 自动首轮新增短超时策略，单个网站不能再连续占用数分钟；最新一轮从既有断点完成 900 条、36 批并以返回码 0 正常退出，当前首轮完成 3,907/20,765、来源待重试 20。处理量比前一轮 525 条提高约 71%，旧主干没有插队。
- 核心回归 60/60、JavaScript 语法通过；采集库只读 `quick_check=ok`、外键异常 0。
- 追加逐链只读交集诊断：Solana 已完成 1,370 条，其中可归属项目证据 21、报价成功 20、公开交集 19；Robinhood 为证据 55、报价 30、交集 30。Arbitrum 126 条已全部完成但项目证据 0；BNB 已完成 621 条但报价成功 0。队列与排名缺陷已修，项目证据覆盖和逐链报价适配仍是下一层真实能力缺口，不能靠降低贝叶斯分数掩盖。
- 唯一启用的长期后台任务仍是 `PenguinConvexity-C1.8-Scheduler`；Gate 0 任务禁用，未发现第二个服务、开机启动项或业务调度器，未重跑 459 万历史底座。
- 完整根因、修复、任务盘点和程序边界见 `docs/C2.4_CHAIN_PIPELINE_REPAIR_20260813.md`。

## 2026-08-14 D0开发体系重置0正式冻结

- 用户明确回复“按冻结稿冻结开发体系重置0”；D0 是工程治理阶段，不是 C2.5，不修改当前 C2.4 产品版本。
- 冻结现场为 `main` 分支 154 项状态：45 项已跟踪改动、109 项未跟踪文件；Git 共 2 个提交，最新正式提交停留在 C2.2。该快照只用于 D0 分类起点，不代表 154 项可以整体提交。
- 正式范围由 `docs/D0_FREEZE_DRAFT.md`、`docs/D0_DEVELOPMENT_WORKFLOW.md`、`docs/D0_ACCEPTANCE_PLAN.md` 和 `docs/D0_REQUIREMENTS_LOCK.json` 固定，需求集 SHA-256 为 `48316081f5851afad07674a54dbc733b261bf64d3bb28d1a8ea60fde9f87fc3b`。
- 用户已确认未来可在另行执行授权后使用 `F:\codex项目\企鹅投研\凸性-worktrees` 作为代码专用临时 worktree 根目录；不得复制正式数据库、凭据或运行状态。
- D0 将分类保全当前源码、重建去敏 C2.4 Git 基线、建立当前真相入口、自动门禁、固定候选和代码回滚流程；重建基线不宣称六链或其他业务问题全部正确。
- 本次冻结没有开始分类、清理、创建 worktree、提交、合并、打标签或推送 GitHub；没有修改产品代码、数据库、后台任务、调度器、快捷方式或用户设置。
- 当前最高优先级：等待用户另行明确回复“开始开发体系重置0”。在获得该授权前，不开始 C2.5、六链业务修复或 D0 执行。

## 2026-08-14 D0开发体系重置0开发自测

- 用户明确回复“开始开发体系重置0”；本轮只完成去敏基线、工作区、当前真相、需求追踪、门禁和开发自测，没有修改 C2.4 产品代码、规则、数据库、调度器、桌面入口或用户设置。
- 冻结时 154 个状态项已经全部登记：45 个已跟踪修改、109 个未跟踪状态项、未知项 0、删除项 0；`desktop-host/` 内 1,129 个生成文件已登记但未提交，只保存 20 个正式桌面源码文件。
- C2.4 重建基线提交为 `2aa3c200766382b031b4cb0f9de40efafe245533`；D0 实现位于 `codex/d0-development-reset`，正式 `main` 尚未合并，未创建正式标签，未推送 GitHub。
- F01-F08 门禁正反向测试 8/8 通过；候选公开边界扫描覆盖 600 个文件，禁止路径 0、高置信密钥命中 0。
- A01-H06 共 48 条要求中，开发阶段 36 条已有证据；真实桌面、代码回滚、正式 main、标签和远端核对共 12 条仍待独立终验与发布阶段完成。
- 当前最高优先级：等待用户明确开始 D0 独立终验；开发自测不能替代真实桌面验收、回滚演练或发布授权。
