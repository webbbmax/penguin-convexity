# Gate 0｜Sol 独立终验与放行

状态：`passed_released_with_documented_preexisting_database_baseline_exception`  
终验模型：`gpt-5.6-sol / xhigh`，双元数据一致  
正式运行：`gate0-solfinal-20260809T045924Z-f7bbd2`

## 结论

Gate 0 的可恢复 90 天数据预检工具和冻结协议范围数据已通过 Sol 独立终验并提升为正式 Gate 0 结果。C2.1 产品需求仍未冻结、未编码；本次通过不等于评分算法、前台产品或全市场母池已经可用。

正式结果覆盖六链、262/262 个分片，固定窗口为 `2026-05-10T14:07:10Z` 至 `2026-08-08T14:07:10Z`。共保留 5,922,807 条窗口内真实创建事件，形成 4,585,955 个唯一候选。全部候选均能独立重算回最早覆盖事件。

## Sol 终验修复

- 清除 353 条固定窗口外事件：BNB 333、Robinhood 2、Solana 18；最终窗口外事件为 0。
- 请求账本移除 3,304 条重复尾部记录，并把 212 次上游结果截断改为 `source_failure`，不再冒充成功。
- 冻结 `unsupported` 边界为 Ethereum 4、Base 8、Arbitrum 6，不让后续影子标签漂移进支持范围。
- 修正六链候选分项计数、完成后再次触发归零累计请求的问题，以及首个 Solana 边界分片指向临时目录的问题。
- 最终化过程没有发出网络请求，没有重跑已接受 29 天区间。

## 独立证据

- 262 个分片完成清单与事件文件哈希全部一致。
- 事件重复、解码不完整、缺池、缺代币、缺时间、窗口外和未来时间均为 0。
- 4,585,955 个候选唯一、有事件反向引用，最早覆盖事件重算差异为 0。
- 真实再次触发 `PenguinConvexity-Gate0-Backfill` 后任务结果为 0，阶段为 `completed_idempotent_exit`；请求仍为 6,723，候选、账本、摘要、验证和 262 个分片清单哈希全部不变。
- 四组 Gate 0 自动测试与 Python 编译检查全部通过。
- 进度页和数据报告已重建；响应式结构通过静态 HTML/CSS 契约测试。应用内 Browser 的本地文件安全策略阻止了 `file://` 四视口打开，本次未使用其他浏览器或本地服务器绕过该策略。

## 保护边界

- C2.0 的 5/5 冻结文档和 5/5 核心规则文件哈希一致；评分、动作、L0-L5 和产品代码未改。
- 接受 29 天运行的 6/6 文件及旧 `.tmp` 取证文件哈希、字节数不变。
- C1.8 调度保持启用、Ready、每小时触发、隐藏动作不变；用户暂停仍为 `true`。
- RWA、旧整合项目和旧源项目运行时引用为 0。

冻结时数据库哈希为 `3a30e13c…`，终验时为 `f8000b94…`，因此“物理哈希完全相等”这一字面检查不成立。取证显示最后写入来自终验前的既有产品守护记录 `watchdog-recovery-c2e0eb094109a07c`；Gate 0 使用只读 SQLite URI，终验前后哈希稳定，完整性为 `ok`、外键异常 0。本次按先前 Sol 纠正报告的既定处理，将其记录为 Gate 0 之外的既有业务状态变化，不擅自回滚生产库。

## 产品边界

Gate 0 证明的是：冻结登记协议范围内，可以自动、可恢复地获得 90 天真实 DEX 创建证据，并形成可重复计算的候选母池。它没有证明：

- 已覆盖全市场所有 DEX；
- 池创建时间必然等于代币全局首次公开流通时间；
- 项目证据、安全数据、交易质量和强线索五因子已经齐备；
- C2.1 可以直接开始编码。

14 个不同自然日仍只是实时管道稳定性的并行证据，不是项目观察期，不要求项目或开发等待，也不阻塞下一步产品范围判断。

## 正式产物

- `docs/GATE0_ACCEPTANCE_MANIFEST.json`
- `runtime/gate0-shadow/backfill/latest.json`
- `runtime/gate0-shadow/backfill/background/runs/gate0-solfinal-20260809T045924Z-f7bbd2/sol-independent-audit.json`
- `reports/gate0-backfill-progress/report.html`
- `reports/gate0-data-preflight/report.html`

一次性 `PenguinConvexity-Gate0-Backfill` 恢复任务已在放行后禁用，避免每 15 分钟无意义唤醒；任务定义仍保留，可恢复，不影响 C1.8 产品调度。

下一步只允许 Sol 根据本次真实覆盖结果补齐 C2.1 的正式支持范围、不可实现边界和单版本需求，待用户明确冻结后才可进入产品编码。
