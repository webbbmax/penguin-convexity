# 企鹅投研-凸性｜C2.2 第二轮独立最终验收

验收时间：2026-08-11

结论：C2.2 冻结范围 39/39 项继续通过；第二轮发布后的更新中心可控性修复已于 2026-08-11 17:28 完成复验并发布，18:12 完成现役任务隔离与后台解耦维护，20:39 完成候选生产化覆盖修复独立终验并发布，版本继续保持已发布状态。

## 候选生产化覆盖修复

1. 4,590,214 条历史候选不再要求在一次模型任务或一次不可恢复长进程中处理。程序已经具备固定分片、原子检查点、可恢复游标、单实例锁、失败重试和更新中心可见进度；本次只发布程序，没有启动正式历史扫描。
2. 生产化规则保持 C2.2 既有宽硬门槛与筛选—跟踪职责不变。独立固定样本 10/10、候选生产化专项测试 12/12、全量非发布 Python 测试 83/83、两种桌面视口 4/4 通过。
3. 独立终验发现并修复一项缺失值语义缺陷：上游已经索引交易池但买卖笔数未返回时，不再误写成“等待交易形成”，而是保存为 `source_pending/no_data`，买卖笔数保持 `null`，不进入后台跟踪。
4. 正式启动接口在未获授权时返回 HTTP 409，请求前后生产记录、分片、运行记录和资格批次均为 0。正式历史扫描仍需用户另行明确授权。
5. 正式桌面快捷方式完成冷启动、重复启动单实例、窗口状态保存及关闭后恢复；测试结束恢复用户原窗口状态。页面另在 1180×760、1440×900 下验证历史分母、0 进度、未授权提示和禁用按钮，无 POST、失败请求、控制台错误或横向溢出。
6. 采集库迁移前备份为 `backups/c2.1-pipeline-pre-candidate-production-20260811T200500.db`，SHA-256 `71adf4864e57d5fd00164f0dc0894a2aa1faf904687bbdd9aaa673eee4290327`；迁移只建立生产化结构，未生成候选生产结果。

## 现役任务隔离与后台解耦维护

1. 更新中心只把“90天新币筛选”和“凸性跟踪更新”作为 C2.2 现役作业。旧“机器状态与结论发布”等任务保留在“历史主干维护（不影响 C2.2 前台）”，历史失败默认折叠、不计入现役故障、不再要求用户重试以恢复 C2.2 前台。
2. 凸性跟踪不再调用旧 `full_refresh`，正式入口改为 `c2_2_convexity_tracking_refresh`。组件白名单为证据健康、持续证据、正式项目市场与退出、数据主干、到期跟踪；机器评分、机器结论、催化发布、项目发现、身份复核和旧页面重建均不在现役任务中。
3. 专属跟踪的组件阶段和心跳只写 `runtime/c2.2/jobs/convexity_tracking.json`；旧 `/api/update-status` 只供历史维护区读取，旧失败不能覆盖 C2.2 实时进度。
4. 主库副本完成一次真实专属运行，耗时约 111 秒，状态 `success`、错误数 0；最新运行 `run_source_stats` 中机器结论记录为 0。GoPlus 的部分无返回对象保留为来源级 `partial_success`，没有被伪造成全任务程序失败。
5. 正式两库和正式运行状态在自动测试及副本验收前后未改变。1180×760、1440×900 共 4 项 Edge 真实渲染检查通过，POST 为 0；历史维护卡片另生成两张局部截图并完成视觉检查。
6. 正式桌面快捷方式完成服务重载、冷启动和重复启动单实例；`/api/health` 返回 `experienceRelease=C2.2`、`status=ready` 且已载入专属跟踪任务。

## 发布后更新中心修复

1. “90天新币筛选”和“凸性跟踪更新”两页均恢复独立的立即手动更新、仅手动/自动频率、暂停自动、暂停当前任务和实时进度；两页共用隐藏、单实例、可恢复流水线，但设置与作业状态互不覆盖。
2. 来源状态按作业重新归属。当前新币筛选需要处理 0 条、能力边界 103 条；凸性跟踪需要处理 0 条、能力边界 20 条。`no_data` 与 `unsupported` 不再被叫作“后台来源异常”，也不再把同一批记录同时显示在两页。
3. 继承的工作台明细和单项重试继续保留：平时收起；底层任务运行、失败或需要恢复时自动展开。累计 496 条历史重试记录不再冒充当前故障；当前真正待处理的是“机器状态与结论发布”1项。
4. 查明完整测试曾把“模拟外部信源超时”写入正式运行状态。测试现已隔离，正式状态已按数据库证据恢复为真实程序错误；82个测试文件运行前后，主库、采集库和正式运行状态三个哈希均逐字节不变。
5. “机器状态与结论发布”对历史异常数据类型增加缺失降级，异常字符串不再触发 `.get` 崩溃；真实主库副本复算 626 个案例成功。验收没有在正式数据库上执行重试，当前恢复入口仍由用户决定是否点击。
6. Edge 页面验收 4/4 通过：两种桌面视口、两页手动按钮、实时进度、来源归属、工作台自动展开、无横向溢出、无控制台/网络错误、POST 请求 0。正式快捷方式启动和重复启动单实例通过；Windows 桌面控制助手两次返回 `Transport closed`，未使用旧坐标冒险补点。

## 本轮实际修复

1. C2.2 快照原先晚于 C2.0/C2.1 旧渲染器载入，详情返回和 `pageshow` 可能再次触发旧渲染器并访问不存在的控件。现已把 C2.2 快照前置，并让三个旧渲染器在 C2.2 页面不再接管渲染。
2. 桌面启动器原先可能把仍在运行的 C2.1 服务误判为当前版本。健康检查现同时核验 `experienceRelease=C2.2`、`/api/c2.2/status` 和 C2.2 页面资源。
3. C2.2 单实例锁原先用 `os.kill(pid, 0)` 探测 Windows 进程，存在误终止风险。现改为 Windows 原生只读进程句柄检查，并新增活锁拒绝、陈旧锁恢复测试。

## 冻结范围与继承

- 需求集 SHA-256：`e689e0177a83918e5075c03f524ab9220ae070d193dad7c580ca35ea91c87770`。
- 八份 C2.2 正式文件和五项继承依赖逐项重算，全部匹配需求锁。
- 未批准删除数为 0；没有新增移动端、.NET/WebView2、钱包、自动交易、第二个调度器或 Gate 0 全量重扫。
- 新币筛选与凸性跟踪继续是两个独立作业，只通过稳定 `assetId`、`candidateBuildId` 和原子快照交接。

## 数据与快照

- 正式发布节点主库 `data/convexity.db`：498,814,976 字节，SHA-256 `d301e24f9bf0a4d589a1d7a0ce4a52336aa2a6aaf458d60017e8b9e7a5816382`，623 个项目、625 个机会案例，完整性 `ok`、外键异常 0。发布后自然运行的当前主库为 541,958,144 字节、624 个项目、626 个机会案例，SHA-256 `a7b68e3eb341df98fbb9803c7565a8776f63813bfd206ae0a3f3711da5e7727b`；维护验收前后不变。
- C2.1/C2.2 采集库 `data/c2.1-pipeline.db`：3,558,342,656 字节，SHA-256 `71adf4864e57d5fd00164f0dc0894a2aa1faf904687bbdd9aaa673eee4290327`，4,590,214 条候选记录、348 条评估记录、4,877 条市场观察，完整性 `ok`、外键异常 0。
- 正式发布备份仍位于 `backups/c2.2-release-20260811T1544`；它与实现前基线逐字节一致，不拿历史发布哈希冒充当前运行哈希。
- 当前前台、筛选、跟踪、后台构建分别为 `c22-front-ef89574633a4aaf6`、`c22-screening-cbd15922e18904f4`、`c22-tracking-27efa701b477da6e`、`c22-admin-f43776946eb95f1d`。
- 11 个宽硬门槛通过项目全部进入前台并全部有跟踪对象；当前 5 个“活跃项目”、6 个“持续观察”、0 个“凸性线索”、0 个“等待首次跟踪”。
- 45 条重要变化均来自已发布快照并带稳定项目身份；前台没有把 `data_limited` 当项目分类。

## 更新、恢复与调度

- 正式 C2.2 编排入口以隔离模式按“筛选后跟踪”运行完成，使用真实已发布输入生成同一组 `candidateBuildId`，没有为了验收重复运行上游长任务或写数据库。
- 两个作业设置均保持自动、每 24 小时、未暂停；改变一方设置不会改变另一方。
- 系统只有一个启用的长期任务 `PenguinConvexity-C1.8-Scheduler`，动作继续通过 `wscript.exe` 隐藏启动；没有桌面黑框。
- 原子快照部分发布失败会恢复上一组完整文件；活锁拒绝第二写入者，陈旧锁可恢复；筛选或跟踪失败均保留上一份完整结果。

## 自动化与真实桌面

- 全量 Python：82/82 个测试文件通过。
- C2.2 核心与恢复测试：27/27 通过；C2.1 继承验收：7/7 通过。
- 实时页面和资源：32/32 通过；C1.9 体验回归通过；C2.2 JavaScript 语法全部通过。
- 正式桌面快捷方式冷启动成功；重复启动只有一个产品窗口；关闭重开恢复窗口位置与大小；桌面启动前后两个数据库哈希不变。
- 机会列表进入详情再返回，链筛选、分页上下文和滚动恢复正常；直接业务页面没有 JavaScript 错误和横向溢出。
- 浏览器验收工具在桌面 iframe 外壳记录到一条无页面 URL 的 `MutationObserver` 注入错误；直接业务页无同类错误，真实桌面路径正常，因此判定为验收工具包装层现象，不是产品业务错误。

## 如实保留的产品边界

- 当前 11 个前台项目全部是 C 类“项目关系未确认”，不是 11 个身份已确认项目。
- 当前凸性线索为 0 是真实算法结果，不用放宽规则或模板补位。
- 7/14/30 天时间外校准目前各只有 1 个可用结果，均显示“样本不足”；程序没有因此自动改参数。
- 发布后第一轮自然增量是运行观察项，不阻塞本次代码发布；失败时后台必须显示具体来源状态，前台继续使用上一份完整快照。

发布证据见 `docs/C2.2_ACCEPTANCE_MANIFEST.json` 和 `docs/C2.2_RELEASE_MANIFEST.json`。

## 2026-08-12 候选库真实首轮跟踪闭环修复

1. 复核确认上一轮“资格批次交接修复”只把候选交给了规则计算，并未把候选交给市场、100美元卖出报价、风险与供应、路径四采集；旧主库 624 个项目的跟踪任务不能替代候选库第二关。本次新增有断点的 `candidate_tracking_records`，让两者职责如实分开。
2. “已有首轮结果”原先实际只代表存在规则评估，现改为“首轮跟踪完成”；后台另列“待确认项目身份”。D 类只在后台等待身份确认，不再冒充等待第二关采集的前台项目。
3. T0、生产资格时间、生产记录更新时间或规则哈希晚于当前评估时，旧评估自动失效；修复前 2026-08-12 04:49 的结果不会继续覆盖 06:21 后的权威 T0。
4. 真实候选小批量运行共处理 8 个已具备 A/B/C 身份与前台合同的候选，另补齐 1 个尚未进入历史生产分片但已经在前台的继承项目；最终 7 个当前前台项目首轮跟踪完成，重复运行选择 0 个，证明断点有效。
5. 当前前台结果为：活跃项目 2、观察中 5、凸性线索 0、等待首轮跟踪 0。0 条凸性线索不是交接断链：ArmaraOS 形成交易与流动性路径，heyAura 形成路径四，均只有 1 条；冻结规则要求至少 2 条且必须包含交易与流动性，因此都不构成凸性线索。
6. 真实桌面路径验证更新中心、机会首页和 ArmaraOS 详情页。详情页显示 11 条市场历史、3 条供应历史、7 条风险历史和四路径原因；页面控制台错误 0。未点击任何更新按钮，未触发第二个调度器。
7. 历史候选扫描在安全断点暂停，备份与源库均为 4,591,622 条候选、2,803,897 条生产记录且 `quick_check=ok`。迁移和小批量采集后已从原分片恢复，历史已完成分片 561 个，未重跑 Gate 0 或已完成分片。
8. 本次没有修改 C2.1 数值门槛、四条强路径定义、动态贝叶斯规则、主库结构、C2.3 桌面容器或前台信息架构。C2.4 继续暂停。

## 2026-08-12 发布后维护复验

- 修复桌面启动器耗时状态误判、候选状态全量读取锁、历史与日常双写入、恢复重复准备和资格交接重复全量评估。
- 真实日常筛选完成并发布，历史扫描从保存断点恢复；没有重跑 Gate 0、修改主库、改变规则或新增调度器。
- 现场数据：主库 624/626；采集库 4,591,622 条候选、2,173,467 条生产记录、487 个资格批次、12,810 条当前评估；前台 9 个项目。
- 候选专项 19/19、独立样本 10/10、运行 10/10、快照 4/4、C2.2 验收 8/8 通过；两库完整性 `ok`、外键异常 0；真实桌面启动、单实例、窗口保存与恢复通过。
