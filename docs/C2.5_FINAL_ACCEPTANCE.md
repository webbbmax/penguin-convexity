# 企鹅投研-凸性｜C2.5 完整独立终验

结论：固定候选 `887bdaf35e3c93c05b688745264d9cc1c68874d6` 完整独立终验通过，92/92 项冻结要求具备实现、测试和独立证据，阻断项为 0。

## 1. 固定对象与范围

- 基线：`becd256b170b0a3a1a45de0b8c4f4a426eb9065a`。
- 终验候选：`887bdaf35e3c93c05b688745264d9cc1c68874d6`。
- C2.5 需求锁、12 份冻结文件、10 项继承依赖、D0 门禁、开发前就绪产物和候选 Git blob 来源全部匹配。
- C2.4 的 29 个既有路由批准删除 0；筛选、四路径、贝叶斯、动作、六链业务能力、数据库结构、唯一调度器和 C# 业务边界未改变。

## 2. 独立结果

- D0 acceptance gate 13/13 通过。
- 完整受影响自动化 147/147 通过；失败恢复、审计、重启、回滚和 J05 高风险定点组 8/8 通过。
- 规则重放 18,574 条，受影响 17,857 条；18 条受治理规则逐项集合的总体并集与独立重算一致。
- current/rollback 均与 C2.4 真实执行器逐资产一致，直接差异 0、缺失 0、计算错误 0；5/5 候选治理快照完整。
- 两个正式数据库仅以只读方式核验，`quick_check=ok`、外键异常 0；未执行正式管理 POST 或业务数据库写入。
- 40/40 页面路由、8/8 C2.5 GET API 通过；慢渲染期间规则、判断链和快照页始终由 C2.5 渲染器持有，没有回退旧通用渲染器。
- 真实 WPF/WebView2 覆盖启动、关闭、单实例、故障、端口冲突、浏览器保留、后台隔离、键鼠和窗口操作；100%、125%、150% DPI 及减少动画均通过。

## 3. 恢复与污染检查

- Windows 显示缩放恢复为原 100%，动画效果恢复为原开启状态，候选窗口状态恢复原哈希。
- 终验结束时无企鹅桌面宿主或 8766/8877 测试监听残留。
- 候选和正式仓库均保持干净；正式调度器 XML、桌面快捷方式和 C2.2 更新配置哈希未漂移。

## 4. 非阻断观察

- 首次无预热服务的旧 C2.3 烟测把 6.381 秒冷页面就绪套入 5 秒热路径阈值；实际热服务路径为 0.803 秒，独立生命周期冷启动为 6.321 秒，低于 I01 的 30 秒门槛。
- 生命周期测试夹具曾出现结果目录未预建和一次关闭超过脚本 5 秒等待；最终完整复跑全部场景通过，进程、端口、WebView2 和窗口状态均恢复。

## 5. 元数据收口说明

终验固定候选在开发阶段预先把 A03、I01-I08、J04-J07 留为 `pending`，导致发布门禁无法读取已经完成的独立结果。用户于 2026-08-18 明确授权创建仅含终验和发布元数据的后继候选并定点复验后发布。

元数据后继提交不得改变 `app/`、`scripts/`、`fixtures/` 或 `desktop-host/` 的 Git tree；发布前必须重新验证这些 tree 与 `887bdaf...` 完全一致，并重新通过 `release_preflight`。
