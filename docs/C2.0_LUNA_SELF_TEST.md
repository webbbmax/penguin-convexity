# C2.0 Luna 开发自测报告

日期：2026-08-03（Asia/Shanghai）  
模型门禁：`turn_context.model=gpt-5.6-luna`、当前线程设置模型 `gpt-5.6-luna`，推理强度 `max`，一致。

## 通过项

### 机器测试

- 全部 `scripts/test_*.py`：59 个脚本，59 通过，0 失败。
- `python scripts/test_c2_0_decision_quality.py`：快照身份、前台安全字段、首页上限、项目/案例对账、七类漏斗、核心哈希、数据库只读完整性、`after_front` 失败回滚和 JS 语法均通过。
- `python scripts/test_complete_user_flow.py --live`：31 个页面或资源 HTTP 200，静态路由、资源、桌面路由和核心用户路径通过。
- `node --check`：`app/c2-front.js`、`app/c2-quality.js`、`app/workbench-nav.js` 通过。
- `python -m py_compile`：新增/修改的 C2 构建器、服务、更新链和测试脚本通过。

### 真实本地服务

- `GET /api/health`：`ready`；体验版本保持 C1.9；启动后快照重建 `success`。
- `GET /api/c2.0/status`：`success`，最后快照 buildId 为 `c2.0-0c61e3e1b49a497d`，项目 600、案例 602、首页信号 0/5。
- 当前 SQLite：`integrity_check=ok`、外键异常 0、业务表 54；项目/案例及原始事件、标准事件、证据、溯源、阶段历史和跟踪执行数量已现场核验。
- 原子失败注入：`python scripts/build_decision_quality_snapshots.py --simulate-failure after_front` 非 0 退出，前台和后台活动快照 SHA-256 保持不变，临时文件清理；随后已重新生成成功快照，运行状态恢复 `success`。

### 浏览器交互自测

使用本地浏览器连接真实服务，保持业务数据只读：

1. 全部机会搜索 `Aave` 后结果收敛为 3 个项目；展开“为什么排在这里”后排序理由可见。
2. 重要变化第一条事件链初始 `open=false`，点击后变为 `open=true`，中间步骤可见。
3. 从全部机会进入 Aave 详情，详情标题可见；点击“返回全部机会”后搜索词仍为 `Aave`，结果计数恢复。
4. 判断质量点击“主体身份”已闭环段后，队列筛选变为 `identity/closed`，队列显示 0 条；点击阻断“前往处理”进入 `action-gaps.html`，返回后质量筛选仍保留。

### 截图与视觉

- `docs/C2.0_SCREENSHOT_MANIFEST.json` 含设计规格要求的 17 类截图。
- 覆盖真实 0 信号、1 个有效信号、过期沿用、筛选与排序理由、事件链折叠/展开、单位正常/口径不明、完整/不完整详情、七类漏斗、阻断/队列、对账失败、1280×720、1440×900、1920×1080、125% 缩放和后台唯一版本号位置。
- Edge headless 渲染截图已生成；隔离 fixture 仅用于状态展示，已在截图生成后删除，不属于产品路由。

## 现场质量观察

- 当前真实数据的首页有效信号为 0/5，质量漏斗中“价值捕获”和“最大可控亏损”仍主要处于待系统处理；这是当前数据质量状态，不是 C2.0 合成信号。
- 前台摘要覆盖率当前为 0/600，模板占比 600/600；后台保留对应指标、单位问题和模板问题队列，没有把不完整项目伪装成完整摘要。
- 前台显示的数值均由带单位/比较口径的变化展示优先提供；口径不明的精确值不在前台输出。

## 未完成或需 Sol 终验

- Luna 未执行最终发布，不更新 `docs/STATUS.md`、最终验收清单或发布状态。
- 桌面宿主的冷启动、单实例、窗口记忆和关闭重开路径未在本轮重新做独立人工点击；既有机器回归通过，但需 Sol 按 C2.0 验收计划独立复验。
- Sol 仍需独立检查 17 类截图、四种视口、WCAG 对比度/键盘焦点、后台处理页原路返回，以及真实业务数据只读边界。

## Luna 交接结论

C2.0 常规实现和开发自测通过，阶段应切换为 `luna_self_test_complete_waiting_sol`；现在停止编码并交由 Sol 终验、缺陷修复和发布。
