# 企鹅投研-凸性｜C2.5 F01 管理者总览热修独立终验

结论：固定产品候选 `5b0cac9a7fe3a26be9d9d9b8951e2a1e84ac83ca` 独立终验通过，可按用户本次授权执行元数据收口并发布。

## 缺陷与修复边界

- 修复前 `/api/c2.5/control-plane` 实测约 31.9—38.8 秒，管理者总览长期停留“正在读取”，违反 F01 十秒要求。
- 修复后总览只读取权威轻量摘要；完整规则重放和快照数据库检查继续留在对应详情接口。
- 前端在 8 秒停止等待，明确说明发生了什么、影响什么、下一步以及重试不会触发任务或修改数据。
- 仅修改 `app/c2-5-admin.js`、`scripts/c2_5_control_plane.py` 和 `scripts/test_c2_5_experience.py`；没有改变 C2.4 规则、任务、调度器、数据库、桌面容器或 40 个页面路由。

## 独立结果

- C2.5 直接受影响套件 67/67、C2.4 直接依赖 49/49、当前路由 40/40 通过；D0 acceptance 门禁通过。
- 候选服务总览冷读 3.832 秒，后续读取 0.848—0.886 秒。
- 真实 WPF/WebView2 从机会中心进入管理者总览后 1.570 秒完成，调度器、任务、健康、下一次运行和当前规则均可见。
- 隔离浏览器阻塞总览 GET 后，8.546 秒显示失败原因、影响和安全重试；解除阻塞后 1.322 秒恢复。
- 完整规则和快照详情分别约 8.163 秒、22.731 秒；它们不再阻塞管理者总览，详情能力未删除。
- 两个正式数据库仅用 SQLite `mode=ro` 检查，`quick_check=ok`、外键异常 0；没有执行管理 POST。

## 旧测试处置

扩展旧 C2.4 套件时，快照和真实样本测试因代码 worktree 按 D0 规则不含正式数据库与历史报告而不适用；另有一个静态断言仍硬编码 C2.4 七组导航，与 C2.5 已冻结的八类管理信息域冲突。它们没有作为通过项计算，也没有用低优先级通过数抵消；当前 C2.5 合同、正式库只读检查、40 路由和真实桌面已提供对应替代证据。

## 发布和恢复

- 原正式发布提交 `c979fba77028ee694042ebcbe0740d1fbb2c8234` 是本次代码回滚点；回滚只涉及代码与入口，不覆盖业务数据库或用户设置。
- 元数据收口提交必须保持三个产品文件与 `5b0cac9...` 的 Git blob 完全一致。
- 终验结束时桌面宿主、测试浏览器页、8766/8877 测试监听均已清理。

