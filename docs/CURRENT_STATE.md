# 企鹅投研-凸性｜当前真相

更新时间：2026-08-14

## 一句话结论

当前正式软件仍是 **C2.4**。D0 已正式发布；C2.5“管理者控制面”已完成正式需求冻结和开发前就绪收口，但尚未开发、终验或发布。

## 当前版本与代码

- 用户可见业务版本：C2.4 普通用户体验与机会发布重构。
- 下一产品版本：C2.5 管理者控制面，阶段为 `requirements_frozen_waiting_development_authorization`；需求集 SHA-256 为 `6cea572837fe9a9e0c77f1d3ec7604081cb6f366d3c3ad72e92f037cbd58ed60`。
- 正式目录：`F:\codex项目\企鹅投研\凸性`。
- 正式分支：`main`；C2.5 开发必须继承的最新 C2.4 源基线为 `65cafa7525e2a4a34e6362204c370d10fa058e1f`，正式标签为 `c2.4-desktop-acceptance-cleanup-20260814`。
- 重要事实：正式软件文件已经是 C2.4，但 D0 开始前 `main` 仍停在旧提交并混有 154 项状态；不能把该旧提交冒充完整 C2.4。
- C2.4 重建基线提交：`2aa3c200766382b031b4cb0f9de40efafe245533`，提交说明明确包含 `reconstructed-baseline`。
- D0 通过的代码候选为 `f061bbf717a57849e2fb9b65079fa63264784e2c`，独立终验证据提交为 `7922fd550200a73283fe5d782e89320ff6ecbce2`。
- D0 正式发布标签：`c2.4-reconstructed-baseline-d0`；其带注释标签唯一解析到最终 D0 发布提交。
- `origin/main` 现场仍为 `4c0953fca9911a401766f0d3869bb22f355062a1`，落后本地 C2.4 正式源基线 4 个提交；本次开发前收口没有推送授权，不以远端旧提交覆盖本地基线。
- C2.6 两份规划稿已原样隔离到 `codex/c2-6-planning` 的 `e617f3fff889db7d53954eba6739ad0119ed718a`，不属于 C2.5 基线。
- 回滚点：C2.4 重建基线 `2aa3c200766382b031b4cb0f9de40efafe245533`；D0 不写业务数据库的回滚演练已通过。

## 正式产品入口

- 桌面快捷方式：`C:\Users\Donutfi\Desktop\企鹅投研-凸性.lnk`。
- 快捷方式仍指向正式目录的 `.NET 10 + WPF + WebView2` 桌面程序。
- 本地服务端口：8766。
- 当前正式页面、29 个既有角色路由和两个业务作业继续以 C2.4 为准。

## 正式数据与后台任务

- 正式业务库：`data/convexity.db`。
- 90 天候选与跟踪流水线库：`data/c2.1-pipeline.db`。
- 唯一 Windows 调度器：`PenguinConvexity-C1.8-Scheduler`。
- D0 工作区不含正式数据库、运行状态、凭据或调度配置副本。
- D0 不启动 Gate 0、459 万历史扫描或 90 天全量扫描。

## 已知问题

- C2.4 重建基线只保存当前事实，不证明六条链的覆盖、筛选结果或投资判断正确。
- Robinhood、Solana 以及其余链的候选覆盖仍需后续按链独立排查；D0 不修复这些业务问题。
- 管理者控制面尚未完整展示全部后台任务、规则和逐链状态；该问题已经进入 C2.5 冻结范围，但在开发和发布前仍是现状缺口。
- 完整清单见 `docs/D0_KNOWN_ISSUES.md`。

## 当前阶段与下一授权

- 当前阶段：D0 已发布；C2.5 正式需求已冻结，开发前正式基线、20个生产POST入口补充映射、21项任务目录、92项需求追踪、版本门禁和六组夹具设计已收口，等待用户另行授权整版开发。
- 92项追踪当前全部为 `pending`；这表示实现尚未开始，不得把就绪审计通过解释为产品验收通过。
- C2.5 冻结不改变用户可见业务版本，也不代表管理者控制面已经实现，更不代表六链覆盖或投资判断已经正确。
- 下一合法动作只有用户明确回复“开始 C2.5 整版开发”；六条链逐链修复仍需另行规划、冻结和授权。

## 真相优先级

发生冲突时依次以：用户最新明确决定 → 当前版本需求锁 → 本文件与 `PRODUCT_BASELINE.md` → `STATUS.md` → 历史文档为准。历史记录不删除，但不能覆盖当前状态。
