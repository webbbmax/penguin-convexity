# C2.2 候选生产化覆盖修复：独立终验记录

状态：独立终验通过，程序已发布  
验收时间：2026-08-11 20:13—20:39（Asia/Shanghai）  
正式 459 万历史扫描：未授权、未启动

## 1. 当前结论

独立终验没有直接相信实现报告。重新从冻结需求、迁移前备份和临时数据库固定样本验证后，发现并修复了一个真实缺陷：上游已经索引交易池，但买入/卖出笔数没有返回时，原实现把“未知”写成“等待交易形成”。修复后该情况保存为 `source_pending/no_data`，买入和卖出继续为 `null`，不会进入后台跟踪。

规则、数据库、API、页面和全量非发布回归均已通过。随后从正式桌面快捷方式完成冷启动、重复启动单实例、窗口状态保存和关闭后恢复；真实桌面门禁通过。候选生产化程序现已纳入 C2.2 发布清单，但正式 459 万历史候选扫描仍未授权、未启动。

## 2. 已通过项目

- 原 C2.2 八份冻结文件、五项继承依赖、候选生产化修复五份冻结文件和两套需求集哈希全部一致。
- 独立固定样本 10/10 通过；候选生产化专项回归 12/12 通过。
- 全量非发布 Python 测试文件 83/83 通过。
- JavaScript 语法、Python 编译和 `git diff --check` 通过；只有既有 CRLF 提示，无空白错误。
- 两库 `integrity_check=ok`、外键异常 0。
- 主库 `data/convexity.db` 保持 541,958,144 字节，SHA-256 `a7b68e3eb341df98fbb9803c7565a8776f63813bfd206ae0a3f3711da5e7727b`。
- 采集库保持 4,590,214 个候选；生产记录、分片、运行记录和资格批次均为 0。
- 迁移前备份完整可读，SHA-256 `71adf4864e57d5fd00164f0dc0894a2aa1faf904687bbdd9aaa673eee4290327`。
- 未授权历史启动 API 返回 HTTP 409；请求前后四类生产计数均为 0。
- 1180×760、1440×900 下两个更新页面共 4/4 通过；无横向溢出、控制台错误、失败请求或 POST。
- 历史区域明确显示 459.02 万导入分母、当前 0 进度、正式全量未授权；启动按钮禁用；凸性跟踪仍为独立页面。
- 唯一长期任务仍为 `PenguinConvexity-C1.8-Scheduler`，通过 `wscript.exe` 隐藏启动；没有第二个长期调度器。
- 正式桌面快捷方式目标指向当前启动器；冷启动成功，重复启动只保留一个窗口，窗口位置与大小保存、关闭后重开恢复均通过。

## 3. 独立发现并修复的问题

### 缺失交易笔数被误判为零活动

复现：固定市场样本返回链、池和资产方向，但 `buys=null`、`sells=null`。  
错误结果：`waiting_for_trades`。  
正确结果：`source_pending`，来源状态 `no_data`，两个数值保持 `null`，`trackingEligible=false`。  
修改：`scripts/candidate_production.py`。  
回归：`scripts/test_candidate_production.py` 和 `scripts/test_candidate_production_independent_acceptance.py`。

## 4. 真实桌面门禁

`F05` 已从 Windows 桌面的正式“企鹅投研-凸性”快捷方式重新执行。脚本先核对快捷方式确实指向本项目当前启动器，再关闭产品窗口完成冷启动；重复启动没有创建第二个窗口；改变窗口位置与大小后，状态文件能够保存，关闭重开后能够恢复。测试结束后恢复用户原窗口状态，并在测试前产品已打开时重新打开产品。

页面内容另以 1180×760、1440×900 两种支持的桌面窗口重新渲染检查，“90 天新币筛选”历史区域明确显示 459.02 万分母、0 进度、正式全量未授权，启动按钮保持禁用；两页无 POST、失败请求、控制台错误或横向溢出。真实快捷方式与页面渲染两组证据共同覆盖本轮桌面门禁。

## 5. 发布与长任务边界

- `docs/C2.2_RELEASE_MANIFEST.json` 已登记候选生产化修复及独立终验证据。
- `docs/C2.2_PHASE.json` 已登记候选生产化修复已通过并发布；C2.2 总体仍保持已发布状态。
- 正式历史扫描授权仍为 `false`，启动状态仍为 `false`。
- 即使后续补验通过并发布程序，也不会自动启动 459 万扫描；仍需用户单独明确授权。

## 6. 发布后的下一门禁

当前程序发布门禁已经完成。若要真正处理 4,590,214 个历史候选，仍需用户另行明确授权正式历史扫描；届时只允许使用已冻结的分片、原子检查点、可恢复游标、单实例锁和可见进度运行，不得把本次程序发布解释为扫描授权。
