# C2.5 X01 项目详情身份热修独立定点复验

生成时间：2026-08-18 19:01:49 +08:00  
复验状态：`independent_acceptance_revalidation_passed_waiting_separate_release_authorization_within_candidate_retention_window`

## 结论

- **PASS，但只在当前精确候选的剩余封存窗口内有效。**
- 验收对象仍为提交 `8f02eb76e70cd9c82dea7188096532a5533f29ab`、服务树 `5c7d261e6ae022e9ca46631f4eb7c75c64b745e6bbab0f272b73adc4e0513d57`、candidate manifest `81527f243eba1b5160a3cebef7001e885fa8551bc0ec8ce8b2b8cd86c5b6b76d` 的同一精确候选，不是重新生成的另一个候选。
- 候选于 `2026-08-18T09:59:17.732964Z` 封存，最晚保留至 `2026-08-19T09:59:17.732964Z`，即北京时间 **2026-08-19 17:59:17.732964**。本次检查时剩余 82,648 秒，约 22 小时 57 分钟。
- 若用户发布授权或实际发布发生在上述时间之后，本 PASS 自动失效，必须重新生成候选，并重新执行候选绑定、X01 70/70、6/6及必要人工定点复验；不得沿用本报告。
- 本次未修改 X01 产品代码，未合并 `main`，未打标签，未发布，未更新正式入口、快捷方式、调度器或 C2.6 继承基线。

## 原 NO-GO 历史与本次授权边界

原独立终验没有被覆盖或改写：

| 原证据 | SHA-256 | 原结论 |
|---|---|---|
| `docs/C2.5_X01_PROJECT_DETAIL_IDENTITY_HOTFIX_FINAL_ACCEPTANCE_20260818.md` | `f5f8d03b1b0745c2cb3c88f87ff298b09d93f133a2ddfb2a98ab6ceb8e9b24c2` | NO-GO |
| `docs/C2.5_X01_PROJECT_DETAIL_IDENTITY_HOTFIX_ACCEPTANCE_MANIFEST_20260818.json` | `6f7da6ba56016f92f58cd27ee0fad1440d9ac3b353d3d9f07aa51e1a53612751` | `no_go_candidate_handoff_and_formal_database_immutability_gates_failed` |

本次门槛调整来自产品路线审核及用户明确授权“按现有24小时候选合同定点复验”，不是终验任务自行放宽：

1. 按当前 D0/C2.5 合同，X01 精确候选只需在现行 24 小时封存窗口内保持 sealed、未过期且字节不变；不要求本热修扩建永久候选体系。
2. 正式数据库判断对象改为候选、隔离服务、隔离 WPF 与终验操作是否造成写入。既有唯一调度器按原配置且能独立归因的变化，不再单独判定候选失败。

因此，本报告是原 NO-GO 之后的独立定点复验结论，不否定原报告当时对额外门槛的事实记录。

## 候选来源、差异与精确字节绑定

| 项目 | 独立复验结果 |
|---|---|
| 验收 HEAD | `8f02eb76e70cd9c82dea7188096532a5533f29ab` |
| 候选分支 | `codex/c2-5-x01-project-detail-hotfix-20260818`；同一提交也为当前独立验收分支 HEAD |
| 正式回滚基线 | `36c974061d616628b1a10e27670f207d29e51f79` |
| 正式标签 | `c2.5-f01-manager-overview-hotfix-20260818`，剥离后精确指向 `36c9740...` |
| candidate-product-state | SHA-256 `da78896a4a5d0baef3caa4313545ccc8b93608228e67b17fc4ce25cc8779ca0b` |
| candidate manifest | SHA-256 `81527f243eba1b5160a3cebef7001e885fa8551bc0ec8ce8b2b8cd86c5b6b76d` |
| 服务树 | 127 个文件；SHA-256 `5c7d261e6ae022e9ca46631f4eb7c75c64b745e6bbab0f272b73adc4e0513d57` |
| Git app 树 | 89 个文件；SHA-256 `6096c1ececf4d4d1fc953babc72a718a58d0256549d05372fac57dcfd443ad8d` |
| `c2-4-front.js` | Git、候选服务文件、实际 HTTP 响应均为 `a4829781db88ae3c171ba8c277f1281ac7e28a10825d50221a384e21e34d443b` |
| `c2-4-front-snapshot.js` | 523,756 字节；候选服务文件与 manifest 均为 `9e4b65e99462c3b2aacb5a2301635557401f4481ffd038ebd712ee819b9885e7`；canonical 内容哈希为 `12de2a7d...` |

相对正式基线仅有以下四项差异，无额外产品差异：

```text
M app/c2-4-front.js
A docs/C2.5_X01_PROJECT_DETAIL_IDENTITY_HOTFIX_CANDIDATE_MANIFEST_20260818.json
A docs/C2.5_X01_PROJECT_DETAIL_IDENTITY_HOTFIX_DEVELOPMENT_20260818.md
A scripts/test_c2_5_x01_project_detail_identity.js
```

独立遍历同时验证了 manifest 内全部 127 个服务文件、89 个 Git app 文件，以及除动态快照外的服务文件与 Git blob；不是只验证清单自洽。

精确候选产物为：

`runtime/temp-artifacts/c2-5-pre-release-candidate-product-20260818T095723Z-0d8be7ca`

复验前后 `_inspect_payload` 均得到 470,307,759 字节、最新 payload mtime ns `1787047157727963400`，与封存标记完全一致；状态为 `sealed`，未过期，`ownerPid=null`，无占用或 blocked。

## X01 定点自动复验

从上述封存候选直接指定脚本和候选快照，命令可复制：

```powershell
$candidate = 'runtime/temp-artifacts/c2-5-pre-release-candidate-product-20260818T095723Z-0d8be7ca/product-state/app'
$front = Join-Path $candidate 'c2-4-front.js'
$snapshot = Join-Path $candidate 'c2-4-front-snapshot.js'
node scripts/test_c2_5_x01_project_detail_identity.js --front-script $front --snapshot $snapshot
node runtime/temp-artifacts/c2-5-x01-independent-acceptance-20260818-20260818T104814Z-6e8991a3/x01_identity_edge_acceptance.js $front $snapshot
```

结果：

- 70/70 卡片 canonicalName、assetId、detailHref、实际查询参数和详情标题一致；强制样本 Wrapped Gonka、cap、OpenServ、HALO 全部通过。
- 6/6 额外边界通过：有效旧 `id`；无参数；未知 `assetId`；`assetId` 覆盖冲突 `id`；未知 `assetId` 不回落到有效 `id`；空 `id`/空 `assetId` 不形成 `null` 匹配。
- 边界脚本 SHA-256 为 `786db0b7653a0a66839aaa0e77f0943ca21b5febce5c2d740c78576316d2baea`，已随本复验证据封存。

原终验的 C2.5 直接相关 66/66、C2.4 规则与跟踪 41/41、C2.4 静态继承 12/12、40 路由 40/40、WPF 宿主 12/12、D0 单测 15/15、D0 acceptance gate 和候选只读探针均绑定同一 candidateCommit、manifest、服务树、Git app 树、前端脚本和快照；本次候选未漂移，因此这些证据未失效，按授权不重复全量。

## 同一隔离候选的 WPF/WebView2 人工点击

隔离服务仍使用上述封存候选，命令为：

```powershell
python scripts/serve_local.py --port 8877
```

隔离 WPF 使用独立互斥体、命名管道、运行目录和 WebView2 user-data，未接管正式桌面。桌面日志记录：

- 18:53:06 启动并复用身份匹配的 `8877` 隔离服务；
- WebView2 Evergreen `151.0.4129.86`；
- 18:55:11 正常关闭，`exitCode=0`。

人工从普通用户“全部机会”路径完成两次点击：

| 卡片 | History 实际 URL 身份 | 视觉详情标题 | 结论 |
|---|---|---|---|
| cap | `assetId=project-asset-a4eb1921402d669b` | `cap` | 内容归属正确 |
| Wrapped Gonka | `assetId=candidate-asset-bd53294c98f8c37a197742fb` | `Wrapped Gonka` | 内容归属正确 |

独立截图：

- `visual/revalidation-cap-detail.png`，SHA-256 `66f4cc3907872f9e3b6207ccc3730519a3aab391972015d0bfedc5265b73b7e3`
- `visual/revalidation-wrapped-gonka-detail.png`，SHA-256 `88bc08cc30f31daf5f6e3483727060b76c23ed24bae89c7e06a8201d03d48a5e`
- WebView2 History 副本 `visual/WebView2-History`，SHA-256 `e4109ec4b884375177c1ac693eb73535bd5bcb7cedf1e7e47830756f2de87498`

候选服务空闲时只有 2 个普通文件句柄、监听 `127.0.0.1:8877`，正式数据库句柄为 0；WPF 主进程及可检查的 WebView2 子进程正式数据库句柄均为 0。一个受沙箱保护的 WebView2 子进程拒绝进程句柄枚举，但 WebView2 只消费 `8877` HTTP，正式数据库只可能由服务端打开；服务端实现使用 `mode=ro` 并及时关闭连接，且实际服务进程无正式数据库句柄。

本复验只点击普通用户导航、卡片详情与窗口关闭；未调用管理 POST，未点击立即运行、暂停、恢复、重试、规则批准或其他写控制。

## 正式数据库写入归因与只读边界

再次使用 SQLite URI `mode=ro`、`PRAGMA query_only=ON` 复核两库：

| 数据库 | quick_check | 外键异常 | 只读探针自身改变大小/mtime |
|---|---:|---:|---:|
| `data/convexity.db` | `ok` | 0 | 否 |
| `data/c2.1-pipeline.db` | `ok` | 0 | 否 |

既有唯一调度器独立证据：

- Windows 任务 `PenguinConvexity-C1.8-Scheduler` 的 XML UTF-16 SHA-256 为 `c2308aa9e73b03511d497341fb408e5e8d92a789897741e59478fbcc883c9f42`，仍按 `PT15M` 运行；动作仍为 `wscript.exe ...run-c2-1-update-hidden.vbs C:\Python312\python.exe`。
- 18:45:01 任务启动，正式 scheduler-state 记录 `lastStartedAt=18:45:02`、`trigger=automatic`；pipeline DB mtime 为 18:46:09；运行在 18:50:53 正常结束，Task Scheduler 结果为 0。
- 本次 WPF 窗口为 18:53:06—18:55:11，晚于 pipeline DB 的 18:46:09 mtime；候选服务和 WPF 均无正式数据库写句柄。
- 最终边界复核时，19:00:01 的下一次既有调度也已按原配置完成并返回 0；此时候选服务和 WPF 均早已停止，进一步排除了候选对后续正式写入的因果关系。

因此，终验窗口内正式 pipeline DB 的变化可独立归因于既有调度器；没有无法归因的写入，也没有终验或候选触发写入。本任务未暂停、启动、停止或修改调度器。

## 正式边界与临时产物

- 正式 `main` 仍为 `36c974061d616628b1a10e27670f207d29e51f79`，工作树干净。
- 正式 `8766` 在复验前后均不可达；本任务没有启动、停止或修改它。
- 正式快捷方式 `C:\Users\Donutfi\Desktop\企鹅投研-凸性.lnk` 仍为 1,412 字节、SHA-256 `034e47abf6aa4935a376e2f03016812b35895c6491ad4103e035027bf76f1e23`，与 `docs/D0_DESKTOP_ACCEPTANCE.json` 相符，目标仍指向正式发布宿主。
- 当前验收工作树除原 NO-GO 两份文件与本次新增两份独立复验文件外无其他变更；未修改 C2.6 规划或继承基线。
- 本次复验证据位于 `runtime/temp-artifacts/c2-5-x01-independent-acceptance-20260818-20260818T104814Z-6e8991a3`，已于 `2026-08-18T11:01:20.273394Z` 封存 24 小时，共 742,904,327 字节，`ownerPid=null`。
- 该证据首次封存因临时 WPF `bin/obj` 深层构建中间目录触发 Windows 路径长度阻断；只清理了可再生成的构建中间目录，保留 `wpf-host-publish`、截图、History、日志和复验脚本后封存成功。项目临时产物状态为 registered 3、due 0、inUse 0、blocked 0、unregistered 0。

## 回滚点、剩余风险与权限

- 回滚/发布基线仍为 `36c974061d616628b1a10e27670f207d29e51f79`；正式标签仍为 `c2.5-f01-manager-overview-hotfix-20260818`。
- 唯一实质剩余风险是候选的时间边界：候选快照不在 Git，当前 PASS 依赖上述精确候选在北京时间 2026-08-19 17:59:17.732964 前仍未过期、未漂移。
- 本 PASS 只表示可在剩余候选窗口内申请单独发布授权，不构成发布授权，不授权合并、打标签、发布、更新正式入口、快捷方式、调度器或 C2.6 继承基线。

当前应停止本任务，等待产品路线审核和用户单独发布授权。
